---
phase: 09-admin-ui-overhaul
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - app/super-admin/models/page.tsx
  - app/super-admin/organizations/page.tsx
  - app/super-admin/super-admins/page.tsx
  - app/super-admin/users/page.tsx
  - app/super-admin/api-keys/page.tsx
  - app/super-admin/analytics/page.tsx
  - app/super-admin/audit-logs/page.tsx
  - app/super-admin/settings/page.tsx
  - app/super-admin/system-prompt/page.tsx
  - app/org/[slug]/admin/page.tsx
  - app/org/[slug]/admin/instructions/page.tsx
  - app/org/[slug]/admin/roles/page.tsx
  - app/org/[slug]/admin/mcp/page.tsx
  - app/org/[slug]/admin/users/page.tsx
  - app/org/[slug]/admin/invitations/page.tsx
  - app/org/[slug]/admin/security/page.tsx
  - app/org/[slug]/admin/settings/page.tsx
  - app/org/[slug]/admin/analytics/page.tsx
  - app/org/[slug]/admin/audit-logs/page.tsx
  - app/org/[slug]/admin/conversations/page.tsx
  - components/full-chat-app.tsx
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04, POLISH-05, POLISH-06, NAV-04]

must_haves:
  truths:
    - "All admin pages use the shared AdminPageHeader component with title + optional description + optional actions"
    - "No admin page renders SidebarTrigger or decorative icons in the page header"
    - "Data-heavy pages (users, models, audit logs, etc.) have flat table layouts without Card wrappers"
    - "Settings-style pages (instructions, settings, security, system prompt, MCP) use single-column centered layout with max-w-3xl"
    - "All admin pages scroll properly when content overflows (flex h-screen flex-col + flex-1 overflow-auto)"
    - "Consistent spacing, typography, and visual hierarchy across all admin pages in both dashboards"
    - "Org admins see an Admin Console link in the chat sidebar profile dropdown menu"
    - "Non-admin users do NOT see the Admin Console link"
  artifacts:
    - path: "app/super-admin/models/page.tsx"
      provides: "Reference data-heavy page with AdminPageHeader"
      contains: "AdminPageHeader"
    - path: "app/super-admin/settings/page.tsx"
      provides: "Reference settings-style page with centered layout"
      contains: "max-w-3xl"
    - path: "components/full-chat-app.tsx"
      provides: "Chat sidebar with Admin Console in dropdown"
      contains: "Admin Console"
  key_links:
    - from: "app/super-admin/models/page.tsx"
      to: "components/admin/admin-page-header.tsx"
      via: "import AdminPageHeader"
      pattern: "import.*AdminPageHeader"
    - from: "components/full-chat-app.tsx"
      to: "app/org/[slug]/admin/page.tsx"
      via: "Admin Console dropdown link"
      pattern: "Admin Console"
---

<objective>
Apply Vercel-level visual polish to all admin pages across both dashboards and add the Admin Console link to the chat sidebar dropdown.

Purpose: Every admin page gets consistent headers, proper scroll behavior, and clean layouts matching Vercel Dashboard aesthetics. The bidirectional navigation between chat and admin is completed.
Output: All ~20 admin pages updated with AdminPageHeader, proper layout containers, and consistent styling. Chat sidebar dropdown includes Admin Console for org admins.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-ui-overhaul/09-CONTEXT.md
@.planning/phases/09-admin-ui-overhaul/09-RESEARCH.md
@.planning/phases/09-admin-ui-overhaul/09-01-SUMMARY.md

@components/admin/admin-page-header.tsx

<interfaces>
<!-- AdminPageHeader from Plan 01 -->
From components/admin/admin-page-header.tsx:
```typescript
interface AdminPageHeaderProps {
  title: string
  description?: string
  actions?: React.ReactNode
}
export function AdminPageHeader({ title, description, actions }: AdminPageHeaderProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish all Super Admin pages and all Org Admin pages with AdminPageHeader and layout fixes</name>
  <files>
    app/super-admin/models/page.tsx,
    app/super-admin/organizations/page.tsx,
    app/super-admin/super-admins/page.tsx,
    app/super-admin/users/page.tsx,
    app/super-admin/api-keys/page.tsx,
    app/super-admin/analytics/page.tsx,
    app/super-admin/audit-logs/page.tsx,
    app/super-admin/settings/page.tsx,
    app/super-admin/system-prompt/page.tsx,
    app/org/[slug]/admin/page.tsx,
    app/org/[slug]/admin/instructions/page.tsx,
    app/org/[slug]/admin/roles/page.tsx,
    app/org/[slug]/admin/mcp/page.tsx,
    app/org/[slug]/admin/users/page.tsx,
    app/org/[slug]/admin/invitations/page.tsx,
    app/org/[slug]/admin/security/page.tsx,
    app/org/[slug]/admin/settings/page.tsx,
    app/org/[slug]/admin/analytics/page.tsx,
    app/org/[slug]/admin/audit-logs/page.tsx,
    app/org/[slug]/admin/conversations/page.tsx
  </files>
  <action>
For EACH admin page, apply the appropriate layout pattern. Read each file first, then apply the changes.

**General changes for ALL pages:**
1. Import `AdminPageHeader` from `@/components/admin/admin-page-header`
2. Remove any `SidebarTrigger` import and usage
3. Remove any decorative icon in the page header (e.g., `Cpu` icon in models page)
4. Remove the old custom header markup
5. Replace with `<AdminPageHeader title="..." description="..." actions={...} />`
6. Wrap the page in `<div className="flex h-screen flex-col">` with AdminPageHeader as the first child
7. Put content in `<div className="flex-1 overflow-auto ...">` as the second child
8. Remove any Card wrappers around tables (use flat layout with the table directly)
9. Remove any unwanted borders or visual artifacts
10. Use semantic color classes only (text-foreground, bg-background, border-border, etc.)

**Data-heavy pages** (models, organizations, super-admins, users, api-keys, audit-logs, invitations, conversations, org admin users):
```
<div className="flex h-screen flex-col">
  <AdminPageHeader title="..." description="..." actions={...} />
  <div className="flex-1 overflow-auto p-6">
    {/* error banner if exists */}
    {/* loading state or table content -- NO Card wrapper */}
  </div>
</div>
```

Action buttons (Add, Refresh, Export, etc.) go in the `actions` prop of AdminPageHeader.

**Settings-style pages** (settings, system-prompt, security, instructions, mcp, org settings):
```
<div className="flex h-screen flex-col">
  <AdminPageHeader title="..." description="..." />
  <div className="flex-1 overflow-auto">
    <div className="mx-auto max-w-3xl px-6 py-6 space-y-8">
      {/* Sections stacked vertically with clear headings */}
    </div>
  </div>
</div>
```

**Dashboard pages** (analytics, org admin dashboard overview):
```
<div className="flex h-screen flex-col">
  <AdminPageHeader title="..." description="..." actions={...} />
  <div className="flex-1 overflow-auto p-6 space-y-6">
    {/* KPI cards + charts */}
  </div>
</div>
```

**Specific page notes:**
- `super-admin/models`: Remove Cpu icon, SidebarTrigger. Title: "Model Registry", description: "Manage AI models available on the platform"
- `super-admin/organizations`: Title: "Organizations", description: "Manage tenant organizations"
- `super-admin/super-admins`: Title: "Super Admins", description: "Manage platform administrators"
- `super-admin/users`: Title: "Users", description: "Cross-organization user management"
- `super-admin/api-keys`: Title: "API Keys", description: "Manage platform API keys"
- `super-admin/analytics`: Title: "Analytics", description: "Platform-wide usage analytics"
- `super-admin/audit-logs`: Title: "Audit Logs", description: "Platform activity logs"
- `super-admin/settings`: Title: "Platform Settings", description: "Configure platform defaults". Use centered single-column layout
- `super-admin/system-prompt`: Title: "System Prompt", description: "Configure the platform-level system prompt". Use centered single-column layout
- `org admin/page.tsx` (dashboard): Title: "{orgName} Admin" or "Admin Dashboard", description: "Organization administration overview"
- `org admin/instructions`: Title: "Instructions", description: "Configure AI system instructions for your organization". Centered layout
- `org admin/roles`: Title: "Roles", description: "Manage roles and permissions"
- `org admin/mcp`: Title: "MCP Servers", description: "Manage Model Context Protocol connections". Centered layout
- `org admin/users`: Title: "Members", description: "Manage organization members"
- `org admin/invitations`: Title: "Invitations", description: "Manage user invitations"
- `org admin/security`: Title: "Security", description: "Password policy and security settings". Centered layout
- `org admin/settings`: Title: "Settings", description: "Organization settings and configuration". Centered layout
- `org admin/analytics`: Title: "Analytics", description: "Organization usage analytics"
- `org admin/audit-logs`: Title: "Audit Logs", description: "Organization activity logs"
- `org admin/conversations`: Title: "Conversations", description: "Manage and review conversations"

**Important anti-patterns to avoid:**
- Do NOT wrap tables in Card components
- Do NOT add icons to page titles
- Do NOT add SidebarTrigger to any page
- Do NOT hardcode hex/oklch colors (use Tailwind semantic classes)
- Do NOT create nested scroll containers
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
- All 20 admin pages use AdminPageHeader with consistent title + description
- No SidebarTrigger or decorative icons in any page header
- Data-heavy pages use flat table layout without Card wrappers
- Settings-style pages use centered single-column layout (max-w-3xl)
- All pages have proper scroll handling (flex h-screen + overflow-auto)
- Consistent spacing and typography across both dashboards
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Admin Console link to chat sidebar dropdown</name>
  <files>components/full-chat-app.tsx</files>
  <action>
In `components/full-chat-app.tsx`, locate the chat sidebar footer area where the user account dropdown menu is rendered (DropdownMenuContent in the ChatSidebar section).

**Changes:**
1. Find the existing standalone Admin Console SidebarMenuItem (likely before the dropdown) that shows for org admins. REMOVE this standalone item entirely.
2. Inside the DropdownMenuContent (which already has Settings and Log out items), add an "Admin Console" DropdownMenuItem for org admins:
   - Only show when `isOrgAdmin && orgSlug` (or equivalent condition already used for the standalone item)
   - Place it ABOVE the Settings item in the dropdown
   - Icon: `Shield` from lucide-react (consistent with admin sidebar)
   - Text: "Admin Console"
   - onClick: navigate to `/org/${orgSlug}/admin`
   - Style: `className="gap-2.5 px-3 py-2"` with `<Shield className="size-4 text-muted-foreground" />`
3. Add a `DropdownMenuSeparator` after Admin Console (before Settings)

This ensures org admins see "Admin Console" in the profile dropdown alongside Settings and Log Out -- bidirectional navigation matching the "Back to Chat" link in the admin sidebar profile expander.

**Important:** Verify the existing condition for checking admin status. The component should already have a way to determine if the user is an org admin (likely from session data or a state variable). Use the same condition.
  </action>
  <verify>
    <automated>npx tsc --noEmit components/full-chat-app.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>
- Admin Console link appears in the chat sidebar dropdown menu for org admins
- Non-admin users do not see the Admin Console link
- Standalone Admin Console button removed from outside the dropdown
- Clicking Admin Console navigates to `/org/{orgSlug}/admin`
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Visit every Super Admin page -- each has AdminPageHeader with title + description, no SidebarTrigger, no decorative icons
3. Visit every Org Admin page -- same consistent headers
4. Settings-style pages (instructions, settings, security, system prompt, MCP) are centered with max-width
5. Data-heavy pages (models, users, audit logs) have flat tables without Card wrappers
6. All pages scroll properly when content overflows
7. In the chat interface, org admins see Admin Console in the profile dropdown; non-admins do not
</verification>

<success_criteria>
- All 20 admin pages use AdminPageHeader consistently
- Three layout patterns applied correctly (data-heavy, settings-style, dashboard)
- No visual noise (unwanted borders, icons in headers, SidebarTrigger in content)
- Proper scroll handling on all pages
- Admin Console link in chat dropdown for org admins only
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-ui-overhaul/09-02-SUMMARY.md`
</output>
