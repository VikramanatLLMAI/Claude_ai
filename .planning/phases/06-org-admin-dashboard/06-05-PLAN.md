---
phase: 06-org-admin-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - app/org/[slug]/admin/analytics/page.tsx
  - components/admin/org-analytics-charts.tsx
autonomous: true
requirements:
  - OUI-04
  - OANA-01
  - OANA-02
  - OANA-03
  - OANA-04
  - OANA-05
  - OANA-06
  - OANA-07
  - OANA-08
  - OANA-09
  - OANA-10
  - OANA-11
  - OANA-12
  - OANA-13
  - OANA-14
  - OANA-15

must_haves:
  truths:
    - "Analytics dashboard shows 4 KPI cards (Active Users, Total Conversations, Total Tokens, Users Near Limits)"
    - "Dashboard has sections for Usage Trends, User Analytics, Model & MCP Usage, Operational Metrics with anchor navigation"
    - "Time range presets (7d, 30d, 90d, 1y) and custom date picker control all chart data"
    - "Each chart section has a CSV export download icon"
    - "Section-based loading shows skeleton loaders per section while data loads"
    - "Analytics page replaces the existing Usage page"
  artifacts:
    - path: "app/org/[slug]/admin/analytics/page.tsx"
      provides: "Comprehensive analytics dashboard with KPIs, 10+ chart sections, time range controls"
    - path: "components/admin/org-analytics-charts.tsx"
      provides: "Org-specific Recharts chart components for all analytics sections"
  key_links:
    - from: "app/org/[slug]/admin/analytics/page.tsx"
      to: "/api/org/{slug}/admin/analytics?section="
      via: "section-based fetch calls in useEffect"
      pattern: "fetch.*admin/analytics"
    - from: "components/admin/org-analytics-charts.tsx"
      to: "recharts"
      via: "AreaChart, BarChart, PieChart, ResponsiveContainer imports"
      pattern: "from 'recharts'"
---

<objective>
Build the comprehensive org analytics dashboard with all 15 OANA metrics, replacing the existing Usage page.

Purpose: Enterprise-grade analytics for Org Admins — KPI overview, usage trends, user analytics, model distribution, MCP usage, operational metrics. Each section loads independently with skeleton loaders. Per-section CSV export enables leadership reporting.

Output: Analytics page with KPI cards and 10+ chart sections, org-specific chart components
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-org-admin-dashboard/06-CONTEXT.md
@.planning/phases/06-org-admin-dashboard/06-RESEARCH.md
@.planning/phases/06-org-admin-dashboard/06-02-SUMMARY.md

@app/super-admin/analytics/page.tsx
@components/admin/analytics-charts.tsx
@components/admin/kpi-card.tsx

<interfaces>
<!-- From 06-02: org-analytics-service response shapes -->
```typescript
// GET /api/org/{slug}/admin/analytics?section=kpi
interface KpiSummary {
  activeUsers: number; suspendedUsers: number; pendingInvites: number
  totalConversations: number; totalMessages: number
  totalTokens: number
  usersNearLimits: number
}

// ?section=trends → { data: UsageTrendPoint[] }
interface UsageTrendPoint { date: string; inputTokens: number; outputTokens: number; thinkingTokens: number }

// ?section=users → { topUsers: TopUser[], nearLimits: NearLimitUser[], inactive: InactiveUser[] }
// ?section=models → { distribution: ModelDist[], avgResponse: AvgResponse[] }
// ?section=roles → { perRole: RoleUsage[] }
// ?section=mcp → { data: McpTrend[] }
// ?section=errors → { data: ErrorItem[] }
// ?section=peak → { data: PeakPoint[] }
// ?section=invitations → { data: InvitationStat[] }
// ?section=apiKeys → { data: ApiKeyUsage[] }
```

From components/admin/kpi-card.tsx:
```typescript
interface KpiCardProps {
  title: string; value: string | number; subtitle?: string
  icon: LucideIcon; trend?: { value: string; positive: boolean }
}
```

From components/admin/analytics-charts.tsx:
// 9 chart components — adapt/reference but create org-specific versions
// Pattern: ResponsiveContainer > Chart > CartesianGrid + XAxis + YAxis + Tooltip + Area/Bar/Pie
// Recharts v3 requires `as any` casts on Tooltip formatter props
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Org analytics chart components</name>
  <files>components/admin/org-analytics-charts.tsx</files>
  <action>
Create `components/admin/org-analytics-charts.tsx` with org-specific chart components. Adapt patterns from the existing `analytics-charts.tsx` (Super Admin charts). Use Recharts v3.7.0 — remember the `as any` cast pattern for Tooltip formatters (decision [05-06]).

Add `/* eslint-disable @typescript-eslint/no-explicit-any */` at top (same as analytics-charts.tsx).

**Chart components to create (Claude's discretion on exact chart types per CONTEXT.md):**

1. **OrgUsageTrendChart** — OANA-07: Stacked AreaChart (input/output/thinking tokens) over time. Same pattern as UsageTrendChart but for org data.

2. **OrgTokensByUserChart** — OANA-03: Stacked bar chart showing token breakdown by user per day/period.

3. **OrgModelDistributionChart** — OANA-04: Horizontal BarChart showing most used models by request count and tokens.

4. **OrgTopUsersChart** — OANA-05: Horizontal BarChart (top 10 users by tokens) + small data table below with name, role, messages, tokens. Table rows could link to Members page.

5. **OrgPerRoleUsageChart** — OANA-06: Bar chart showing per-role token usage with user count annotations.

6. **OrgMcpUsageChart** — OANA-08: AreaChart showing MCP tool call count over time. Empty state if no MCP data.

7. **OrgAvgResponseTimeChart** — OANA-09: Horizontal BarChart showing avg response time per model in ms.

8. **OrgErrorRateChart** — OANA-10: Donut PieChart showing error type distribution. Empty state if no errors.

9. **OrgPeakUsageHeatmap** — OANA-11: CSS grid heatmap (24 hours x 7 days) with color intensity. Same pattern as PeakUsageHeatmap.

10. **OrgInvitationStatusChart** — OANA-12: Donut PieChart showing invitation status counts (Pending/Accepted/Expired/Revoked).

11. **OrgApiKeyUsageChart** — OANA-13: Horizontal BarChart showing usage per assigned API key.

12. **OrgUsersNearLimitsTable** — OANA-14: Small table (not a chart) showing users at 80%+ of their limits. Columns: Name, Role, Usage %, Limit Type.

13. **OrgInactiveUsersTable** — OANA-15: Small table showing inactive users (30+ days). Columns: Name, Email, Role, Last Active, Days Inactive.

Each component:
- Wrapped in Card with CardHeader (title) + CardContent
- Accepts typed data props
- Handles empty state: "No data available for this period"
- Has a small download icon button in CardHeader for CSV export (calls parent's exportSection callback)
- Uses ResponsiveContainer width="100%" height={300-400} for charts
- Color palette: consistent with Super Admin charts (use Tailwind CSS variable colors)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>13 org-specific chart/table components created, all with Card wrappers, empty states, export buttons, and proper Recharts typing</done>
</task>

<task type="auto">
  <name>Task 2: Analytics dashboard page with section-based loading</name>
  <files>app/org/[slug]/admin/analytics/page.tsx</files>
  <action>
Create `app/org/[slug]/admin/analytics/page.tsx` following the Super Admin analytics page pattern.

**Page structure (single scrollable page per CONTEXT.md):**

1. **Header:** SidebarTrigger + BarChart3 icon + "Analytics" title
2. **Time range controls (right side of header):**
   - Preset buttons: 7d, 30d, 90d, 1y (same as Super Admin)
   - Custom date picker (two date inputs: start, end)
   - Refresh button
3. **Section navigation (optional sticky anchor links):**
   - Overview | Usage Trends | User Analytics | Model & MCP Usage | Operational Metrics
   - Clicking scrolls to the section (use element IDs with scrollIntoView)

4. **Sections:**

**Overview (top):** — OANA-01, OANA-02, OANA-14
- 4 KPI cards in a grid (responsive: 1-col mobile, 2-col sm, 4-col lg):
  - Active Users (icon: Users, subtitle: "+X suspended, Y pending")
  - Total Conversations (icon: MessageSquare)
  - Total Tokens Used (icon: Zap, formatted with K/M/B abbreviations)
  - Users Near Limits (icon: AlertTriangle, trend if available)

**Usage Trends:** — OANA-07
- OrgUsageTrendChart (full width)

**User Analytics:** — OANA-03, OANA-05, OANA-06, OANA-14, OANA-15
- OrgTopUsersChart (full width or 2/3)
- OrgPerRoleUsageChart (half width)
- OrgTokensByUserChart (half width)
- OrgUsersNearLimitsTable (full width)
- OrgInactiveUsersTable (full width)

**Model & MCP Usage:** — OANA-04, OANA-08, OANA-09, OANA-13
- OrgModelDistributionChart (half width)
- OrgAvgResponseTimeChart (half width)
- OrgMcpUsageChart (half width)
- OrgApiKeyUsageChart (half width)

**Operational Metrics:** — OANA-10, OANA-11, OANA-12
- OrgErrorRateChart (1/3 width)
- OrgPeakUsageHeatmap (1/3 width)
- OrgInvitationStatusChart (1/3 width)

**Section-based loading (per CONTEXT.md):**
- Each section has its own state (data | null) and loading state
- KPI loads first (fast) via fetchSection('kpi')
- Other sections load in parallel via Promise.all after KPI
- While loading, show Skeleton components matching chart dimensions
- If a section fails, show error state for that section only (not whole page)

**Per-section CSV export:**
- Each chart component has a download icon
- Export triggers fetch to /api/org/{slug}/admin/analytics?section=X&export=csv&startDate=...&endDate=...
- Download via blob+anchor pattern (same as audit log export)
- File naming: org-analytics-{section}-{YYYY-MM-DD}.csv

**Also handle replacement of Usage page:**
- Either delete app/org/[slug]/admin/usage/page.tsx or replace its content with a redirect to /org/{slug}/admin/analytics
- The sidebar already points to /analytics from Plan 06-01
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Analytics dashboard loads all sections with skeleton loaders, time range controls work, per-section CSV export downloads files, old Usage page replaced</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Analytics page loads KPI cards quickly, then charts fill in
- Time range presets (7d/30d/90d/1y) reload all chart data
- Each section shows appropriate chart type
- CSV export produces downloadable file
- /analytics replaces /usage in navigation
</verification>

<success_criteria>
1. All 15 OANA requirements have corresponding UI sections
2. Section-based loading provides fast perceived performance
3. Per-section CSV export works with proper file naming
4. Enterprise-grade presentation with proper empty states
</success_criteria>

<output>
After completion, create `.planning/phases/06-org-admin-dashboard/06-05-SUMMARY.md`
</output>
