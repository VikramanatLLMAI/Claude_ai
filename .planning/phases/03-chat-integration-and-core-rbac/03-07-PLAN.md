---
phase: 03-chat-integration-and-core-rbac
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [prisma/seed.ts]
autonomous: true
gap_closure: true
requirements: [UCHAT-01, UCHAT-02, OMCP-01, OINST-01, OINST-02, ORSI-01, ORSI-02, SAFE-07]

must_haves:
  truths:
    - "admin@acme-corp.test user has org_admin permission in seed data"
    - "requireOrgAdmin middleware passes for admin@acme-corp.test"
    - "Admin Console button visible in chat sidebar for admin user"
    - "Org Admin Console pages accessible at /org/acme-corp/admin/*"
  artifacts:
    - path: "prisma/seed.ts"
      provides: "Org admin permission in Technical role seed data"
      contains: "org_admin"
  key_links:
    - from: "prisma/seed.ts"
      to: "lib/auth-middleware.ts"
      via: "permissions array includes org_admin"
      pattern: "org_admin"
    - from: "lib/auth-middleware.ts"
      to: "app/api/org/[slug]/models/route.ts"
      via: "requireOrgAuth permissions check feeds isOrgAdmin response"
      pattern: "permissions.includes.*org_admin"
---

<objective>
Fix seed data so admin@acme-corp.test has org_admin permission, unblocking all 7 failed UAT tests (Tests 7-13).

Purpose: The requireOrgAdmin middleware checks `permissions.includes('org_admin') || role.name === 'Org Admin'`. The Technical role's permissions array in seed.ts is `['chat', 'mcp', 'artifacts', 'file_upload', 'web_search']` -- missing `org_admin`. This single omission causes every Org Admin Console feature to return 403, and the Admin Console button to be hidden in the chat sidebar.

Output: Updated seed.ts with org_admin permission, re-seeded database.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chat-integration-and-core-rbac/03-UAT.md

Key understanding:
- `lib/auth-middleware.ts` line 348-350: `requireOrgAdmin` checks `permissions.includes('org_admin') || role.name === 'Org Admin'`
- `app/api/org/[slug]/models/route.ts` line 28: `isOrgAdmin` flag computed from same check, sent to frontend
- `components/full-chat-app.tsx` line 583: Admin Console button conditionally rendered when `isOrgAdmin && orgSlug`
- All 7 Org Admin API routes use `requireOrgAdmin` middleware
- The code is correct -- only the seed data is wrong
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add org_admin permission to Technical role in seed data and re-seed</name>
  <files>prisma/seed.ts</files>
  <action>
In `prisma/seed.ts`, find the Technical role creation block (around line 312-328). The current permissions array is:

```typescript
permissions: ['chat', 'mcp', 'artifacts', 'file_upload', 'web_search'],
```

Add `'org_admin'` to this array:

```typescript
permissions: ['chat', 'mcp', 'artifacts', 'file_upload', 'web_search', 'org_admin'],
```

This is the ONLY change needed. Do NOT:
- Create a separate "Org Admin" role (the Technical role is the admin role per decision [02-01])
- Modify any other file (the middleware, API routes, and frontend are all correct)
- Change any other role's permissions (Business and Basic roles should NOT have org_admin)

After editing the file, re-seed the database:
1. Run `npm run db:reset` to drop and recreate the database with fresh schema
2. Run `npx tsx prisma/seed.ts --dev` with the required env vars (SUPER_ADMIN_EMAIL, SUPER_ADMIN_PASSWORD, SUPER_ADMIN_NAME) to re-seed

Note: db:reset will delete all existing data. This is acceptable because this is dev seed data.
  </action>
  <verify>
    <automated>
Run the following to verify the fix:

1. `npx tsx -e "import prisma from './lib/db'; async function main() { const role = await prisma.role.findFirst({ where: { name: 'Technical' } }); const perms = role?.permissions as string[]; console.log('Has org_admin:', perms?.includes('org_admin')); process.exit(perms?.includes('org_admin') ? 0 : 1); } main();"` -- should print "Has org_admin: true" and exit 0

2. `npm run build` -- should compile without errors (verifies no TypeScript issues introduced)
    </automated>
  </verify>
  <done>
- prisma/seed.ts Technical role permissions array includes 'org_admin'
- Database re-seeded with updated permissions
- admin@acme-corp.test user's Technical role now has org_admin permission
- requireOrgAdmin middleware will pass for this user
- isOrgAdmin will be true in /api/org/acme-corp/models response
- Admin Console button will appear in chat sidebar for admin user
- All 7 Org Admin Console pages (/admin, /admin/instructions, /admin/roles, /admin/mcp) will be accessible
  </done>
</task>

</tasks>

<verification>
After re-seeding, verify the full chain:

1. Database: Technical role has org_admin in permissions JSON array
2. API: GET /api/org/acme-corp/models returns `isOrgAdmin: true` for admin@acme-corp.test
3. Frontend: Admin Console button visible in chat sidebar
4. Org Admin routes: /org/acme-corp/admin/* pages load without 403 redirect

This single seed data fix resolves all 7 UAT gaps (Tests 7-13) because they all share the same root cause.
</verification>

<success_criteria>
- admin@acme-corp.test can see Admin Console button in chat sidebar (Test 7)
- admin@acme-corp.test can access /org/acme-corp/admin (Test 8)
- admin@acme-corp.test can access /org/acme-corp/admin/instructions (Tests 9, 10)
- admin@acme-corp.test can access /org/acme-corp/admin/roles (Tests 11, 12)
- admin@acme-corp.test can access /org/acme-corp/admin/mcp (Test 13)
- user@acme-corp.test (Basic role) still cannot access any admin pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-chat-integration-and-core-rbac/03-07-SUMMARY.md`
</output>
