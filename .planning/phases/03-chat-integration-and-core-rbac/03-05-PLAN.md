---
phase: 03-chat-integration-and-core-rbac
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/org/[slug]/admin/roles/page.tsx
  - components/admin/role-model-assignment.tsx
  - app/api/org/[slug]/admin/roles/route.ts
  - app/api/org/[slug]/admin/roles/[roleId]/models/route.ts
  - app/api/org/[slug]/admin/roles/[roleId]/settings/route.ts
autonomous: true
requirements: [OLLM-01, MODL-07]

must_haves:
  truths:
    - "Org Admin can assign which models each role can access, with models grouped by generation and mixed-state group checkbox"
    - "At least one model must be enabled per role (validated at save time)"
    - "Org Admin can toggle custom instructions enabled/disabled per role"
    - "Org Admin can enable/disable personal MCP servers per role with a max count setting"
  artifacts:
    - path: "app/org/[slug]/admin/roles/page.tsx"
      provides: "Role settings management page with model assignment"
      min_lines: 80
    - path: "components/admin/role-model-assignment.tsx"
      provides: "Model assignment UI with generation grouping"
      exports: ["RoleModelAssignment"]
    - path: "app/api/org/[slug]/admin/roles/[roleId]/models/route.ts"
      provides: "Role model assignment endpoint"
      exports: ["GET", "PATCH"]
    - path: "app/api/org/[slug]/admin/roles/[roleId]/settings/route.ts"
      provides: "Role settings endpoint (custom instructions toggle, personal MCP)"
      exports: ["GET", "PATCH"]
  key_links:
    - from: "app/org/[slug]/admin/roles/page.tsx"
      to: "components/admin/role-model-assignment.tsx"
      via: "model assignment component"
      pattern: "RoleModelAssignment"
    - from: "app/api/org/[slug]/admin/roles/[roleId]/models/route.ts"
      to: "lib/services/model-registry-service.ts"
      via: "getAllModels for available models list"
      pattern: "getAllModels"
---

<objective>
Build the Org Admin role settings page with model assignment (generation grouping, mixed-state checkboxes) and role configuration (custom instructions toggle, personal MCP settings).

Purpose: Org Admins need to control which models each role can access and configure role-level settings. Model assignment uses the Platform Model Registry from Plan 01 as the source of truth.

Output: Working role model assignment page with generation grouping and role configuration toggles.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chat-integration-and-core-rbac/03-CONTEXT.md
@.planning/phases/03-chat-integration-and-core-rbac/03-RESEARCH.md
@.planning/phases/03-chat-integration-and-core-rbac/03-01-SUMMARY.md

Key existing files:
@lib/services/model-registry-service.ts (created in Plan 01)
@lib/token-counter.ts (created in Plan 01)
@lib/auth-middleware.ts
@lib/services/audit-service.ts
@lib/validation.ts

<interfaces>
<!-- Key types from existing code and prior plans -->

From lib/services/model-registry-service.ts (Plan 01):
```typescript
export async function getAllModels(status?: 'ACTIVE' | 'DEPRECATED'): Promise<Model[]>;
export async function getModelsGroupedByGeneration(): Promise<Record<string, Model[]>>;
```

From prisma schema (Role, relevant fields):
```prisma
model Role {
  id                         String
  name                       String
  allowedModels              Json     @default("[]")    // array of model ID strings
  customInstructionsEnabled  Boolean  @default(true)
  customInstructionsMaxLength Int     @default(1000)
  personalMcpEnabled         Boolean  @default(false)
  personalMcpMaxCount        Int      @default(3)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role settings APIs (roles list, model assignment, role configuration)</name>
  <files>app/api/org/[slug]/admin/roles/route.ts, app/api/org/[slug]/admin/roles/[roleId]/models/route.ts, app/api/org/[slug]/admin/roles/[roleId]/settings/route.ts</files>
  <action>
  1. Create `app/api/org/[slug]/admin/roles/route.ts`:
     - `GET` -- requireOrgAdmin -> tenantDb.role.findMany({ orderBy: { createdAt: 'asc' } }) -> return roles array with all fields including allowedModels, customInstructionsEnabled, personalMcpEnabled, personalMcpMaxCount

  2. Create `app/api/org/[slug]/admin/roles/[roleId]/models/route.ts`:
     - `GET` -- requireOrgAdmin -> get role by roleId (verify belongs to org) -> return { allowedModels: string[], roleName: string }
     - `PATCH` -- requireOrgAdmin -> validate body ({ allowedModels: string[] }) -> verify at least one model is in the list (minimum model requirement per CONTEXT.md) -> verify all model IDs exist in Model Registry (call getModelsByIds and check count matches) -> update Role.allowedModels -> audit log (action: 'role.models.updated', metadata: { roleId, roleName, allowedModels }) -> return updated role
     - If less than 1 model in allowedModels, return 400 with "At least one model must be enabled per role"
     - Zod schema: `{ allowedModels: z.array(z.string()).min(1, 'At least one model required') }`

  3. Create `app/api/org/[slug]/admin/roles/[roleId]/settings/route.ts`:
     - `GET` -- requireOrgAdmin -> get role by roleId -> return { customInstructionsEnabled, personalMcpEnabled, personalMcpMaxCount }
     - `PATCH` -- requireOrgAdmin -> validate body -> update role fields -> audit log (action: 'role.settings.updated') -> return updated settings
     - Accepted fields: `customInstructionsEnabled` (boolean), `personalMcpEnabled` (boolean), `personalMcpMaxCount` (integer, min 0, max 20)
     - When `personalMcpEnabled` is toggled ON, `personalMcpMaxCount` defaults to 3 if not provided
  </action>
  <verify>
    `GET /api/org/{slug}/admin/roles` returns all roles with full fields.
    `PATCH /api/org/{slug}/admin/roles/{roleId}/models` with valid model IDs succeeds.
    `PATCH /api/org/{slug}/admin/roles/{roleId}/models` with empty array returns 400.
    `PATCH /api/org/{slug}/admin/roles/{roleId}/settings` updates custom instructions toggle.
    `npm run lint` passes.
  </verify>
  <done>
    Role management APIs functional: list roles, update model assignment with minimum-1 validation, update role settings (custom instructions toggle, personal MCP).
  </done>
</task>

<task type="auto">
  <name>Task 2: Role settings page with model assignment UI</name>
  <files>app/org/[slug]/admin/roles/page.tsx, components/admin/role-model-assignment.tsx</files>
  <action>
  1. Create `components/admin/role-model-assignment.tsx` -- model assignment UI per MODL-07 and CONTEXT.md:
     - Fetch available models from `GET /api/admin/models?status=ACTIVE` (all active models from registry)
     - Group models by `generationGroup`: "Claude 4.6", "Claude 4.5", "Claude 4"
     - Each group has a group-level checkbox with 3 states:
       - Checked: all models in group are selected
       - Unchecked: no models in group are selected
       - Indeterminate (mixed): some models in group are selected
     - Clicking group checkbox toggles all models in that group
     - Each model within a group has an individual checkbox
     - Display model info next to each checkbox: display name, model ID (small monospace text)
     - Props: `roleId: string`, `allowedModels: string[]`, `onSave: (modelIds: string[]) => Promise<void>`
     - Save button: calls onSave with current selection. Disabled if no models selected.
     - For the mixed-state checkbox, install `@radix-ui/react-checkbox` if not already installed, or use the shadcn checkbox component. The indeterminate state is set via `checked="indeterminate"` prop.
     - Run `npx shadcn@latest add checkbox` if the checkbox component doesn't exist at `components/ui/checkbox.tsx`

  2. Create `app/org/[slug]/admin/roles/page.tsx` -- role settings management page:
     - Fetch all roles from GET /api/org/[slug]/admin/roles
     - For each role, show a card/section with:
       a. Role name + description (read-only for Phase 3 -- editing is Phase 4)
       b. **Model Assignment** section with RoleModelAssignment component
       c. **Custom Instructions** section: Switch toggle for `customInstructionsEnabled`. When enabled, shows "Users in this role can write custom instructions (max 200 tokens)". When disabled, shows "Custom instructions disabled for this role".
       d. **Personal MCP Servers** section: Switch toggle for `personalMcpEnabled`. When enabled, shows a number input for `personalMcpMaxCount` (pre-fills with 3 per CONTEXT.md). When disabled, MCP settings section is hidden for users in this role.
     - Save buttons per section (or a single save per role card)
     - Loading skeleton while fetching
  </action>
  <verify>
    Navigate to `/org/{slug}/admin/roles` -- shows all roles (Technical, Business, Basic).
    Each role card shows model assignment with generation grouping and checkboxes.
    Toggle a group checkbox -- all models in group toggle. Mixed state works when partial.
    Save model assignment -- persists across refresh.
    Try to save with 0 models -- error "At least one model must be enabled".
    Toggle custom instructions -- switch persists.
    Toggle personal MCP -- max count input appears/disappears.
    `npm run lint` passes.
  </verify>
  <done>
    Org Admin can assign models per role with generation grouping and mixed-state checkboxes (OLLM-01, MODL-07). Minimum 1 model enforced. Custom instructions toggle and personal MCP settings per role are configurable.
  </done>
</task>

</tasks>

<verification>
- Role settings page shows all roles with model assignment, custom instructions toggle, personal MCP settings
- Model assignment uses generation grouping with mixed-state checkboxes (MODL-07, OLLM-01)
- Minimum 1 model per role enforced
- Custom instructions toggle persists
- Personal MCP toggle with max count persists
- `npm run lint` passes
</verification>

<success_criteria>
- Org Admin can assign models per role from Model Registry (OLLM-01)
- Models grouped by generation with mixed-state checkboxes (MODL-07)
- Minimum 1 model per role enforced at save time
- Custom instructions and personal MCP toggles configurable per role
</success_criteria>

<output>
After completion, create `.planning/phases/03-chat-integration-and-core-rbac/03-05-SUMMARY.md`
</output>
