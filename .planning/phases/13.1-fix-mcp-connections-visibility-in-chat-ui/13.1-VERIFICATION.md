---
phase: 13.1-fix-mcp-connections-visibility-in-chat-ui
verified: 2026-03-09T09:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification:
  previous_status: passed
  previous_score: 5/5
  uat_gaps_found: 3
  gaps_closed:
    - "Admin-created org-wide MCP connections have source=ORG in the database"
    - "Admin-created role-scoped MCP connections have source=ROLE in the database"
    - "personalMcpEnabled is correctly returned by /api/auth/me for org users in dev mode"
    - "When personalMcpEnabled=false and user attempts to add MCP, an error toast appears"
    - "Edit MCP dialog pre-populates name, serverUrl, and authType from existing connection"
  gaps_remaining: []
  regressions: []
---

# Phase 13.1: Fix MCP Connections Visibility in Chat UI - Verification Report

**Phase Goal:** Fix MCP connections visibility in chat UI -- ensure org-wide and role-scoped MCP tools appear in chat, personal MCPs gated by role permission, source labels shown in UI.
**Verified:** 2026-03-09T09:00:00Z
**Status:** PASSED
**Re-verification:** Yes -- after UAT gap closure (Plan 03 closed 3 UAT failures)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Chat route auto-includes org-wide and role-scoped MCP tools regardless of frontend activeMcpIds | VERIFIED | `app/api/chat/route.ts` lines 154-155 query McpConnection with `source: 'ORG'` and `source: 'ROLE'`, line 167 filters org/role for auto-include, line 178 passes sourceMap to `loadActiveMcpToolsWithDescriptions` |
| 2 | Tool call timeline shows (Org) or (Role) source labels next to admin-managed MCP tool names | VERIFIED | `components/prompt-kit/tool-timeline.tsx` line 30 has `parseToolSource` parsing `mcp__org__`/`mcp__role__` prefixes, used at lines 51, 99, 133, 170 |
| 3 | Settings modal MCP tab shows read-only org/role MCP sections above personal connections | VERIFIED | `components/settings-modal.tsx` imports `McpReadonlyCard` (line 37), renders org connections at line 1323 and role connections at line 1335 |
| 4 | personalMcpEnabled is enforced on both frontend (UI gating) and backend (403 on CRUD) | VERIFIED | Frontend: `settings-modal.tsx` reads `personalMcpEnabled` from `/api/auth/me` (lines 226-227), conditionally renders at line 1345. Backend: personal MCP routes return 403 when disabled. |
| 5 | McpConnection model has explicit source enum (ORG, ROLE, PERSONAL) | VERIFIED | `prisma/schema.prisma` lines 19-23 define `enum McpSource { ORG ROLE PERSONAL }`. Backfill script at `prisma/backfill-mcp-source.ts` exists. |
| 6 | Admin-created MCP connections set source=ORG or source=ROLE correctly (UAT Gap 1 fix) | VERIFIED | `app/api/org/[slug]/admin/mcp/connections/route.ts` line 135: `source: assignmentType === 'role-specific' ? 'ROLE' : 'ORG'` |
| 7 | /api/auth/me returns org/role context with personalMcpEnabled in dev mode (UAT Gap 2 fix) | VERIFIED | `app/api/auth/me/route.ts` lines 60-76: fallback `prisma.orgMember.findFirst` without slug filter, line 88: `personalMcpEnabled: orgMember.role.personalMcpEnabled`. Error toast at `settings-modal.tsx` line 761 on 403. |
| 8 | Edit MCP dialog pre-populates fields from existing connection (UAT Gap 3 fix) | VERIFIED | `components/mcp/mcp-add-dialog.tsx` lines 51-70: useEffect syncs name, serverUrl, authType from editData prop |

**Score:** 8/8 truths verified

### UAT Gap Closure Detail (Plan 03)

| UAT Test | Previous Result | Fix Applied | Verified |
|----------|----------------|-------------|----------|
| 3. Role MCP Read-Only Section | ISSUE (blocker): admin endpoint did not set source field, defaults to PERSONAL | `connections/route.ts` line 135 now sets `source: 'ROLE'` or `source: 'ORG'` on create | Yes -- source field present in create data |
| 4. Personal MCP Gating When Disabled | ISSUE (blocker): /api/auth/me returned no role data in dev mode | `auth/me/route.ts` lines 60-76 fall back to direct orgMember lookup; `settings-modal.tsx` line 761 shows error toast on 403 | Yes -- fallback query exists, toast wired |
| 5. Edit MCP Dialog Pre-Population | ISSUE (major): useState only ran on mount | `mcp-add-dialog.tsx` lines 51-70 add useEffect to sync from editData | Yes -- useEffect with editData dependency exists |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `prisma/schema.prisma` | McpSource enum and source field | VERIFIED | Enum at lines 19-23 |
| `prisma/backfill-mcp-source.ts` | Backfill script | VERIFIED | File exists |
| `app/api/mcp/org/route.ts` | GET endpoint for org-wide MCPs | VERIFIED | File exists |
| `app/api/mcp/role/route.ts` | GET endpoint for role-scoped MCPs | VERIFIED | File exists |
| `components/mcp/mcp-readonly-card.tsx` | Read-only MCP card with source badge | VERIFIED | File exists, imported at settings-modal.tsx line 37 |
| `components/prompt-kit/tool-timeline.tsx` | Source-aware tool name formatting | VERIFIED | parseToolSource at line 30 |
| `app/api/org/[slug]/admin/mcp/connections/route.ts` | source field set on POST create | VERIFIED | Line 135 sets source based on assignmentType |
| `app/api/org/[slug]/admin/mcp/connections/[id]/route.ts` | source field updated on PATCH | VERIFIED | Lines 134, 145 update source on assignment change |
| `app/api/auth/me/route.ts` | Fallback org membership lookup | VERIFIED | Lines 60-76 query without slug when unavailable |
| `components/mcp/mcp-add-dialog.tsx` | useEffect sync from editData | VERIFIED | Lines 51-70 sync form state |
| `components/settings-modal.tsx` | toast import, 403 error toast, personalMcpEnabled gating | VERIFIED | toast import line 4, error toast line 761, gating line 1345 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `admin/mcp/connections/route.ts` POST | Prisma McpConnection.create | source field in create data | WIRED | Line 135: `source: assignmentType === 'role-specific' ? 'ROLE' : 'ORG'` |
| `admin/mcp/connections/[id]/route.ts` PATCH | Prisma McpConnection.update | source in updateData | WIRED | Lines 134 (`'ORG'`), 145 (`'ROLE'`) |
| `app/api/auth/me/route.ts` | Prisma OrgMember | fallback query without slug | WIRED | Lines 63-75 query orgMember by userId only |
| `app/api/auth/me/route.ts` | Response role object | personalMcpEnabled | WIRED | Line 88: `personalMcpEnabled: orgMember.role.personalMcpEnabled` |
| `settings-modal.tsx` | sonner toast | error toast on 403 | WIRED | Line 4 imports toast, line 761 calls toast.error |
| `mcp-add-dialog.tsx` | editData prop | useEffect sync | WIRED | Lines 51-70 sync all form fields |
| `chat/route.ts` | mcp-client.ts | sourceMap parameter | WIRED | Line 163 builds sourceMap, line 178 passes to loadActiveMcpToolsWithDescriptions |
| `settings-modal.tsx` | McpReadonlyCard | org/role MCP rendering | WIRED | Import line 37, rendered lines 1323, 1335 |
| `tool-timeline.tsx` | parseToolSource | source label rendering | WIRED | Function at line 30, used at lines 51, 99, 133, 170 |

### Requirements Coverage

No formal requirement IDs for this phase (urgent fix). All success criteria verified above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | - | - | - | - |

No TODO/FIXME/placeholder comments, no empty implementations, no stub patterns detected in any modified files.

### Human Verification Required

### 1. Role MCP Read-Only Section Visibility (re-test of UAT #3)

**Test:** As an org admin, create a role-scoped MCP connection. Then log in as a user with that role, open Settings > MCP tab.
**Expected:** A "Role Tools" section appears showing the role-scoped MCP as a read-only card with a "Role" badge.
**Why human:** Requires admin MCP creation and role assignment to populate the database with source=ROLE records.

### 2. personalMcpEnabled Gating End-to-End (re-test of UAT #4)

**Test:** Set personalMcpEnabled=false on a role, log in as that role's user, open Settings > MCP tab, attempt to add a personal MCP.
**Expected:** The personal MCP section shows a disabled message. If bypassed via API, a toast error appears.
**Why human:** Requires role configuration and UI interaction to verify the toast message.

### 3. Edit MCP Dialog Pre-Population (re-test of UAT #5)

**Test:** Create a personal MCP connection, then click its edit button.
**Expected:** The edit dialog opens with name, server URL, and auth type fields pre-filled with existing values.
**Why human:** Requires creating an MCP connection and verifying form field values visually.

### 4. MCP Tool Auto-Include in Chat

**Test:** Log in as a user with org-wide or role-scoped MCP connections. Start a chat triggering an MCP tool call.
**Expected:** Org/role MCP tools available without manual activation. Tool calls show (Org) or (Role) labels.
**Why human:** Requires running dev server with MCP servers configured.

### Gaps Summary

No gaps found. All 8 truths verified in the codebase. The 3 UAT failures from the initial verification have been addressed by Plan 03:

1. Admin MCP creation now sets source=ORG/ROLE (was defaulting to PERSONAL)
2. /api/auth/me now falls back to direct org membership lookup in dev mode (was returning no role data)
3. Edit MCP dialog now syncs form state via useEffect (was only initializing on mount)

All original 5 truths still hold (no regressions). All artifacts are substantive, properly wired, and free of anti-patterns.

---

_Verified: 2026-03-09T09:00:00Z_
_Verifier: Claude (gsd-verifier)_
