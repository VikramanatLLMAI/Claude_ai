---
phase: 13.1-fix-mcp-connections-visibility-in-chat-ui
verified: 2026-03-09T06:15:00Z
status: passed
score: 5/5 must-haves verified
---

# Phase 13.1: Fix MCP Connections Visibility in Chat UI - Verification Report

**Phase Goal:** Org-wide and role-scoped MCP connections are auto-included in chat, visible in settings, and source-labeled in tool call displays, with personalMcpEnabled enforced as defense-in-depth
**Verified:** 2026-03-09T06:15:00Z
**Status:** PASSED
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Chat route auto-includes org-wide and role-scoped MCP tools regardless of frontend activeMcpIds | VERIFIED | `app/api/chat/route.ts` queries McpConnection with `source: 'ORG'` and `source: 'ROLE'` in OR clause (lines 154-155), builds sourceMap, separates org/role MCPs (always loaded) from personal MCPs (filtered by activeMcpIds), passes sourceMap to `loadActiveMcpToolsWithDescriptions` |
| 2 | Tool call timeline shows (Org) or (Role) source labels next to admin-managed MCP tool names | VERIFIED | `components/prompt-kit/tool-timeline.tsx` has `parseToolSource` (line 30) parsing `mcp__org__`/`mcp__role__` prefixes, renders `(Org)` or `(Role)` span in JSX (line 198). `components/prompt-kit/tool.tsx` has same pattern (lines 21, 255). |
| 3 | Settings modal MCP tab shows read-only org/role MCP sections above personal connections | VERIFIED | `components/settings-modal.tsx` fetches `/api/mcp/org` (line 263) and `/api/mcp/role` (line 275), renders `McpReadonlyCard` for org connections (line 1317) and role connections (line 1329) above personal section |
| 4 | personalMcpEnabled is enforced on both frontend (UI gating) and backend (403 on CRUD) | VERIFIED | Backend: guardrails in `connections/route.ts` POST (line 56), `[id]/route.ts` PATCH+DELETE (lines 72, 152), `[id]/discover/route.ts` POST (line 26), `[id]/test/route.ts` POST (line 20). Frontend: `settings-modal.tsx` reads `personalMcpEnabled` from `/api/auth/me` (line 225), conditionally renders disabled message (line 1397) or personal MCP section (line 1339) |
| 5 | McpConnection model has explicit source enum (ORG, ROLE, PERSONAL) | VERIFIED | `prisma/schema.prisma` defines `enum McpSource { ORG ROLE PERSONAL }` (lines 19-23) and `source McpSource @default(PERSONAL)` on McpConnection (line 528). Backfill script at `prisma/backfill-mcp-source.ts` derives correct values. `package.json` has `db:backfill-mcp` script (line 16). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `prisma/schema.prisma` | McpSource enum and source field | VERIFIED | Enum at lines 19-23, field at line 528 |
| `prisma/backfill-mcp-source.ts` | Backfill script to derive source from userId/roleId | VERIFIED | 76 lines, queries all connections, derives source, batch updates via transaction |
| `app/api/mcp/org/route.ts` | GET endpoint for org-wide MCPs | VERIFIED | 46 lines, uses requireOrgAuth, rate limiting, sanitized response (no credentials) |
| `app/api/mcp/role/route.ts` | GET endpoint for role-scoped MCPs | VERIFIED | 45 lines, uses requireOrgAuth, filters by role.id, sanitized response |
| `components/mcp/mcp-readonly-card.tsx` | Read-only MCP card with source badge | VERIFIED | 33 lines, shows status dot, name, source badge (Org/Role), tool count, server URL |
| `components/prompt-kit/tool-timeline.tsx` | Source-aware tool name formatting | VERIFIED | parseToolSource at line 30, source labels rendered in JSX at line 198 |
| `components/prompt-kit/tool.tsx` | Source-aware tool name formatting | VERIFIED | parseToolSource at line 21, source labels rendered in JSX at line 255 |
| `components/settings-modal.tsx` | MCP tab with org/role sections and personalMcpEnabled gating | VERIFIED | Imports McpReadonlyCard (line 36), fetches org/role MCPs, renders read-only sections, gates personal section |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/api/chat/route.ts` | `lib/mcp-client.ts` | `loadActiveMcpToolsWithDescriptions` with sourceMap | WIRED | Import at line 4, call with sourceMap at line 178 |
| `app/api/chat/route.ts` | Prisma McpConnection | source-based query separating ORG/ROLE/PERSONAL | WIRED | OR clause with source filters at lines 154-155, org/role auto-include at line 167 |
| `lib/mcp-client.ts` | Tool name prefixing | sourcePrefix parameter on convertMcpToolsToAiTools | WIRED | sourcePrefix param at line 326, toolKey uses prefix at line 338, sourceMap logic at lines 468-469 |
| `components/prompt-kit/tool-timeline.tsx` | Chat streaming response | parseToolSource regex on mcp__org__/mcp__role__ | WIRED | parseToolSource at line 30, used in formatToolName (line 51), getToolIcon (line 99), JSX rendering (line 170) |
| `components/settings-modal.tsx` | `/api/mcp/org` and `/api/mcp/role` | fetch calls for read-only MCP data | WIRED | Fetch calls at lines 263, 275; results rendered via McpReadonlyCard at lines 1317, 1329 |
| `components/settings-modal.tsx` | `/api/auth/me` | personalMcpEnabled flag from role data | WIRED | Extracted at line 225, used for conditional rendering at line 1339 |
| `app/api/auth/me/route.ts` | Role.personalMcpEnabled | Included in response role object | WIRED | personalMcpEnabled at line 68 in role response |

### Requirements Coverage

No formal requirement IDs for this phase (urgent fix). All success criteria from ROADMAP.md are verified above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | - | - | - | - |

No TODO/FIXME/placeholder comments, no empty implementations, no stub patterns detected in any modified files.

### Human Verification Required

### 1. MCP Tool Auto-Include in Chat

**Test:** Log in as an org user whose role has org-wide or role-scoped MCP connections configured. Start a chat that triggers an MCP tool call.
**Expected:** Org/role MCP tools should be available to the AI without the user explicitly enabling them. Tool calls should show (Org) or (Role) labels in the timeline.
**Why human:** Requires running the dev server, having MCP servers configured, and triggering actual tool calls to verify end-to-end streaming behavior.

### 2. Settings Modal MCP Tab Layout

**Test:** Open Settings > MCP tab as a user with org-wide and role-scoped MCPs configured.
**Expected:** Organization Tools section appears first with "Org" badges, then Role Tools with "Role" badges, then personal connections below a separator.
**Why human:** Visual layout verification -- spacing, badge styling, section ordering.

### 3. personalMcpEnabled Disabled State

**Test:** Log in as a user whose role has personalMcpEnabled=false. Open Settings > MCP tab.
**Expected:** Personal MCP section shows "Personal MCP connections are not enabled for your role. Contact your admin." message. Add button is hidden.
**Why human:** Requires role configuration and visual verification of disabled state.

### Gaps Summary

No gaps found. All 5 success criteria from ROADMAP.md are verified in the codebase:

1. Backend auto-includes org/role MCPs via source-based query in chat route
2. Frontend displays (Org)/(Role) labels via parseToolSource in tool-timeline.tsx and tool.tsx
3. Settings modal fetches and displays read-only org/role MCP sections via McpReadonlyCard
4. personalMcpEnabled enforced both backend (403 on 5 CRUD endpoints) and frontend (conditional rendering)
5. McpSource enum exists in schema with backfill script for existing data

All artifacts are substantive (no stubs), properly wired (imports, fetch calls, data flow), and free of anti-patterns.

---

_Verified: 2026-03-09T06:15:00Z_
_Verifier: Claude (gsd-verifier)_
