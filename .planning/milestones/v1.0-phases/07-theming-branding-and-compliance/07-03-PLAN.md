---
phase: 07-theming-branding-and-compliance
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/super-admin/organizations/page.tsx
  - app/org/[slug]/admin/settings/page.tsx
  - components/admin/theme-assignment-panel.tsx
  - components/admin/theme-selector.tsx
  - app/org/[slug]/layout.tsx
  - components/full-chat-app.tsx
autonomous: true
requirements: [OTHM-01, OTHM-02, OTHM-03, OTHM-04]

must_haves:
  truths:
    - "Super Admin can assign themes to an org from the org edit/detail page"
    - "Org Admin can choose active theme from only the assigned themes"
    - "Org Admin cannot see or access unassigned themes (OTHM-02)"
    - "Theme applies to entire org and all users within it (OTHM-04)"
    - "All 5 themes available: claude, vercel, solar-dusk, twitter, violet-bloom (OTHM-03)"
  artifacts:
    - path: "components/admin/theme-assignment-panel.tsx"
      provides: "Super Admin theme assignment UI with checkboxes + default selector"
    - path: "components/admin/theme-selector.tsx"
      provides: "Org Admin theme picker showing only assigned themes"
    - path: "app/org/[slug]/layout.tsx"
      provides: "Server-side theme fetch and data-theme attribute application"
  key_links:
    - from: "app/org/[slug]/layout.tsx"
      to: "app/api/org/[slug]/theme/route.ts"
      via: "Server-side fetch on layout render"
      pattern: "data-theme"
    - from: "components/admin/theme-assignment-panel.tsx"
      to: "app/api/super-admin/organizations/[id]/themes/route.ts"
      via: "PUT fetch for theme assignments"
      pattern: "fetch.*themes"
---

<objective>
Theme assignment UI (Super Admin) + theme selection UI (Org Admin) + org-wide theme application on page load.

Purpose: Complete the theme management flow from Super Admin assignment through Org Admin selection to automatic application for all org users. This is the core theming experience.
Output: Theme assignment panel, theme selector component, org layout with server-side theme application
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-theming-branding-and-compliance/07-01-SUMMARY.md
@app/super-admin/organizations/page.tsx
@app/org/[slug]/admin/settings/page.tsx
@app/org/[slug]/layout.tsx
@components/admin/admin-sidebar.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Super Admin theme assignment panel + Org Admin theme selector</name>
  <files>components/admin/theme-assignment-panel.tsx, components/admin/theme-selector.tsx, app/super-admin/organizations/page.tsx, app/org/[slug]/admin/settings/page.tsx</files>
  <action>
**ThemeAssignmentPanel** (components/admin/theme-assignment-panel.tsx):

Reusable component for Super Admin to assign themes to an org. Used in the organizations page (org edit dialog or org detail section).

- Display all 5 themes as a checkbox grid with theme preview swatches (small colored rectangles showing the theme's primary colors)
- Each theme: checkbox + name + color swatch preview
- Radio button or dropdown for "Default Theme" (must be one of the checked themes)
- Save button calls `PUT /api/super-admin/organizations/{id}/themes` with `{ assignedThemes: string[], defaultTheme: string }`
- Loading state + toast on success/error
- Props: `orgId: string`, `initialThemes?: { themes: OrgThemeAssignment[], activeTheme: string | null }`

**ThemeSelector** (components/admin/theme-selector.tsx):

Org Admin component to pick active theme from assigned themes.

- Fetch assigned themes from `GET /api/org/{slug}/admin/themes`
- Display ONLY assigned themes as selectable cards (OTHM-02: cannot see unassigned)
- Each card: theme name + color swatch preview + "Active" badge on current
- Click to select + confirm → calls `PUT /api/org/{slug}/admin/themes` with `{ activeTheme: string }`
- Toast on success: "Theme updated. Changes apply to all users."
- Props: `orgSlug: string`

**Integration - Super Admin organizations page:**

Add a "Themes" section to the org edit dialog or detail view on `app/super-admin/organizations/page.tsx`. When Super Admin edits an org, include the ThemeAssignmentPanel component. Load current themes on dialog open via `GET /api/super-admin/organizations/{id}/themes`.

**Integration - Org Admin settings page:**

Add a "Theme" section to `app/org/[slug]/admin/settings/page.tsx`. Render the ThemeSelector component. This section shows "No themes assigned by platform admin" if no themes are assigned.
  </action>
  <verify>
    <automated>cd /c/Python_project/Antigravity_folder/chatbot_ui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Super Admin can assign themes to orgs via organizations page. Org Admin can select active theme from assigned themes in settings page. Unassigned themes are not visible to Org Admin.</done>
</task>

<task type="auto">
  <name>Task 2: Org layout theme application (server-side + client-side)</name>
  <files>app/org/[slug]/layout.tsx, components/full-chat-app.tsx</files>
  <action>
**Org layout theme application** (app/org/[slug]/layout.tsx):

The org layout is a server component (or has a server component wrapper). Add theme application so the org theme loads without FOUC:

1. In the org layout, fetch the org's active theme. Since this is a layout, consider two approaches:
   - **If server component:** Fetch theme directly from DB (using Prisma, since it's server-side) and pass to children via a data attribute on the HTML wrapper.
   - **If client component:** Fetch from `/api/org/{slug}/theme` on mount and apply immediately.

2. Apply the theme via `data-theme` attribute on a wrapper div or on `document.documentElement`. The CSS already has `[data-theme="X"]` selectors in globals.css.

3. **Fallback behavior:**
   - If API returns `activeTheme: null` → do NOT set data-theme → defaults to claude theme (the `:root` styles)
   - This handles OTHM-06 (all themes removed → platform default)

4. Create a small client component `OrgThemeProvider` if needed:
   ```typescript
   // Fetches and applies org theme on mount
   // Sets data-theme attribute on document.documentElement
   // Handles null → remove attribute → platform default
   ```

**Chat sidebar logo display** (components/full-chat-app.tsx):

Add org logo display in the chat sidebar header. This is a MINIMAL change to the 86KB file:

1. Read org session data from localStorage (already done for orgSlug).
2. If org has `logoBase64` and `logoDisplayMode`:
   - `ORG_ONLY`: Show org logo only
   - `PLATFORM_AND_ORG`: Show org logo + "LLMatscale.ai" text
3. Display in the SidebarHeader area, before or alongside the "New Chat" button.
4. Logo renders as `<img src={logoBase64} className="h-6 w-auto" />` (native img, not next/image, since base64).

IMPORTANT: This is a surgical change to the sidebar header ONLY. Do NOT modify any other part of full-chat-app.tsx. Keep the change under 20 lines of diff.
  </action>
  <verify>
    <automated>cd /c/Python_project/Antigravity_folder/chatbot_ui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Org theme applies automatically on page load via data-theme attribute. Theme fallback chain works (active → default → platform). Chat sidebar shows org logo based on logoDisplayMode. No FOUC.</done>
</task>

</tasks>

<verification>
- Super Admin sees all 5 themes with checkboxes in org edit
- Org Admin sees only assigned themes in settings
- Selecting a theme applies it via data-theme attribute
- Removing all themes falls back to claude (platform default)
- Chat sidebar shows org logo when configured
- TypeScript compiles
</verification>

<success_criteria>
- Full theme flow works: Super Admin assigns → Org Admin selects → all org users see the theme
- OTHM-01 through OTHM-04 satisfied
- Logo displays in chat sidebar per logoDisplayMode
- No flash of unstyled content on page load
</success_criteria>

<output>
After completion, create `.planning/phases/07-theming-branding-and-compliance/07-03-SUMMARY.md`
</output>
