---
phase: 07-theming-branding-and-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/settings-modal.tsx
  - app/api/user/preferences/route.ts
autonomous: true
requirements: [UTHEM-01, UTHEM-02, UTHEM-03]

must_haves:
  truths:
    - "User can toggle between light, dark, and system mode"
    - "User preference persists across sessions via User.preferences in database"
    - "Light/dark mode is independent from org theme (data-theme attribute vs .dark class)"
    - "Color theme picker is removed from settings modal for regular users"
  artifacts:
    - path: "components/settings-modal.tsx"
      provides: "Settings modal with color picker removed, light/dark/system mode persisted to API"
    - path: "app/api/user/preferences/route.ts"
      provides: "User preference persistence API"
      exports: ["GET", "PATCH"]
  key_links:
    - from: "components/settings-modal.tsx"
      to: "app/api/user/preferences/route.ts"
      via: "fetch to persist theme mode"
      pattern: "fetch.*preferences"
---

<objective>
Remove the color theme picker from settings modal and persist light/dark/system mode to the database.

Purpose: Users should only control light/dark/system mode (UTHEM-01), not org-level color themes. Their preference must persist across sessions (UTHEM-02) and be independent from org theme (UTHEM-03).
Output: Modified settings-modal.tsx, user preferences API endpoint
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@components/settings-modal.tsx
@prisma/schema.prisma
@lib/auth-middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove color theme picker + persist theme mode to API</name>
  <files>components/settings-modal.tsx, app/api/user/preferences/route.ts</files>
  <action>
**settings-modal.tsx changes:**

1. **Remove the color theme picker section entirely.** Find the section that renders the 5 color theme buttons (claude, vercel, solar-dusk, twitter, violet-bloom) using `handleColorThemeChange`. Remove the entire color theme selection grid/section. Keep ONLY the light/dark/system mode toggle.

2. **Remove colorTheme state and handleColorThemeChange function.** Remove the `colorTheme` state variable, `COLOR_THEME_KEY` constant, `handleColorThemeChange` function, and any localStorage reads for color theme. The org theme is now controlled by the org, not the user.

3. **Remove the applyColorTheme call** (the one that sets `data-theme` attribute). The `data-theme` attribute will be set by the org theme system in Plan 03, not by user preference.

4. **Persist theme mode (light/dark/system) to API instead of just localStorage.** When user changes theme mode:
   - Still apply immediately via `applyTheme()` (existing logic for .dark class)
   - Also persist to API: `PATCH /api/user/preferences` with `{ themeMode: "light" | "dark" | "system" }`
   - On settings modal open, read from localStorage for immediate display (fast), but also fetch from API to sync

5. **Keep the existing applyTheme function** that handles .dark class toggling. This is the light/dark/system logic and must remain.

**User preferences API** (app/api/user/preferences/route.ts):

- GET: Return user preferences from `User.preferences` JSON field. Uses requireAuth (works for any authenticated user).
- PATCH: Body `{ themeMode?: "light" | "dark" | "system" }`. Update `User.preferences` JSON field. Zod validate. Uses requireAuth.
- The User model already has a `preferences` JSON field with `themeMode` default.

Note: Check if this API route already exists (there may be a user settings route). If so, extend it rather than creating a new one.
  </action>
  <verify>
    <automated>cd /c/Python_project/Antigravity_folder/chatbot_ui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Color theme picker removed from settings modal. Light/dark/system mode toggle remains and persists to database via API. Theme mode is independent from org theme (UTHEM-03).</done>
</task>

</tasks>

<verification>
- Settings modal no longer shows color theme selector (claude, vercel, etc.)
- Light/dark/system toggle still works and applies .dark class correctly
- Theme mode change calls PATCH /api/user/preferences
- No references to COLOR_THEME_KEY or handleColorThemeChange remain
- data-theme attribute is NOT set by settings modal anymore
</verification>

<success_criteria>
- Color theme picker completely removed from settings modal
- Light/dark/system mode persists to User.preferences via API
- .dark class toggling remains functional and independent from data-theme attribute
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-theming-branding-and-compliance/07-02-SUMMARY.md`
</output>
