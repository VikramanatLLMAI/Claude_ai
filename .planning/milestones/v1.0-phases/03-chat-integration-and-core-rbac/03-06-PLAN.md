---
phase: 03-chat-integration-and-core-rbac
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-04"]
files_modified:
  - app/org/[slug]/admin/mcp/page.tsx
  - components/admin/mcp-assignment-panel.tsx
  - app/api/org/[slug]/admin/mcp/connections/route.ts
  - app/api/org/[slug]/admin/mcp/connections/[id]/route.ts
  - app/api/org/[slug]/admin/mcp/connections/[id]/discover/route.ts
  - app/api/org/[slug]/admin/mcp/connections/[id]/test/route.ts
  - app/api/org/[slug]/user/custom-instructions/route.ts
  - components/settings-modal.tsx
autonomous: true
requirements: [OMCP-01, OMCP-02, OMCP-03, OMCP-04, OMCP-05, UCUST-01, UCUST-02, UCUST-03, UCUST-04, SAFE-08]

must_haves:
  truths:
    - "Org Admin can connect MCP servers and assign them org-wide or to specific roles"
    - "Org Admin can remove or disconnect MCP servers independently per assignment type"
    - "User can write custom instructions in Settings modal (if role permits), with live 200-token counter"
    - "Disabled custom instructions show grayed-out text with 'Custom instructions disabled by your admin' message in Settings modal"
    - "Custom instructions are org-specific (tied to OrgMember, not User)"
  artifacts:
    - path: "app/org/[slug]/admin/mcp/page.tsx"
      provides: "MCP server management page"
      min_lines: 50
    - path: "components/admin/mcp-assignment-panel.tsx"
      provides: "MCP server connection and assignment UI"
      exports: ["McpAssignmentPanel"]
    - path: "app/api/org/[slug]/admin/mcp/connections/route.ts"
      provides: "MCP connection list and create endpoints"
      exports: ["GET", "POST"]
    - path: "app/api/org/[slug]/user/custom-instructions/route.ts"
      provides: "User custom instructions endpoint"
      exports: ["GET", "PATCH"]
    - path: "components/settings-modal.tsx"
      provides: "Settings modal with custom instructions section wired in"
      contains: "custom-instructions"
  key_links:
    - from: "app/org/[slug]/admin/mcp/page.tsx"
      to: "components/admin/mcp-assignment-panel.tsx"
      via: "McpAssignmentPanel component"
      pattern: "McpAssignmentPanel"
    - from: "app/api/org/[slug]/user/custom-instructions/route.ts"
      to: "lib/token-counter.ts"
      via: "token validation"
      pattern: "estimateTokenCount"
    - from: "components/settings-modal.tsx"
      to: "/api/org/[slug]/user/custom-instructions"
      via: "fetch for custom instructions CRUD"
      pattern: "api/org.*custom-instructions"
---

<objective>
Build the Org Admin MCP server management page (connect, assign org-wide or role-specific, test, discover, remove) and wire user custom instructions into the existing settings modal with the InstructionEditor component.

Purpose: Org Admins need to manage MCP tool access by role. Users need to be able to write custom instructions within their role's permitted scope, accessible from the existing Settings modal -- not a standalone unreachable component.

Output: Working MCP management page, user custom instructions API, and custom instructions UI wired into settings-modal.tsx.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chat-integration-and-core-rbac/03-CONTEXT.md
@.planning/phases/03-chat-integration-and-core-rbac/03-RESEARCH.md
@.planning/phases/03-chat-integration-and-core-rbac/03-01-SUMMARY.md
@.planning/phases/03-chat-integration-and-core-rbac/03-04-SUMMARY.md

Key existing files:
@lib/token-counter.ts (created in Plan 01)
@lib/auth-middleware.ts
@lib/services/audit-service.ts
@lib/validation.ts
@lib/mcp-client.ts
@lib/encryption.ts
@components/admin/instruction-editor.tsx (created in Plan 04)
@components/mcp/mcp-add-dialog.tsx
@components/mcp/mcp-connection-card.tsx
@components/settings-modal.tsx

<interfaces>
<!-- Key types from existing code and prior plans -->

From lib/token-counter.ts (Plan 01):
```typescript
export const TOKEN_LIMITS: { org: 700; role: 500; user: 200 };
export function estimateTokenCount(text: string): number;
```

From prisma schema (McpConnection, relevant fields):
```prisma
model McpConnection {
  id        String
  userId    String?     // null = not personal
  roleId    String?     // null = org-wide, set = role-specific
  name      String
  serverUrl String
  isActive  Boolean
}
```

From prisma schema (OrgMember):
```prisma
model OrgMember {
  customInstructions String? @db.Text
}
```

From components/admin/instruction-editor.tsx (Plan 04):
```typescript
interface InstructionEditorProps {
  value: string;
  onChange: (value: string) => void;
  maxTokens: number;
  label: string;
  description?: string;
  disabled?: boolean;
  disabledMessage?: string;
}
export function InstructionEditor(props: InstructionEditorProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP management API routes and page</name>
  <files>app/api/org/[slug]/admin/mcp/connections/route.ts, app/api/org/[slug]/admin/mcp/connections/[id]/route.ts, app/api/org/[slug]/admin/mcp/connections/[id]/discover/route.ts, app/api/org/[slug]/admin/mcp/connections/[id]/test/route.ts, app/org/[slug]/admin/mcp/page.tsx, components/admin/mcp-assignment-panel.tsx</files>
  <action>
  **A. MCP Management API Routes (OMCP-01 through OMCP-05):**

  1. Create `app/api/org/[slug]/admin/mcp/connections/route.ts`:
     - `GET` -- requireOrgAdmin -> tenantDb.mcpConnection.findMany({ where: { userId: null } }) -> return connections (org-managed only, not personal). Include role info if roleId is set.
     - `POST` -- requireOrgAdmin -> validate body (name, serverUrl, authType, authCredentials?, assignmentType: 'org-wide' | 'role-specific', roleId?) -> create McpConnection:
       - If assignmentType is 'org-wide': roleId = null, userId = null (OMCP-02)
       - If assignmentType is 'role-specific': roleId = provided roleId, userId = null (OMCP-03)
       - Encrypt authCredentials if provided (use `encrypt()` from lib/encryption.ts)
       - Audit log: 'mcp.connection.created'
     - Return 201 with connection

  2. Create `app/api/org/[slug]/admin/mcp/connections/[id]/route.ts`:
     - `GET` -- requireOrgAdmin -> get connection by id (verify belongs to org) -> return connection
     - `PATCH` -- requireOrgAdmin -> validate body -> update connection -> audit log -> return updated
     - `DELETE` -- requireOrgAdmin -> delete connection (OMCP-05) -> audit log: 'mcp.connection.deleted' -> return 204

  3. Create `app/api/org/[slug]/admin/mcp/connections/[id]/discover/route.ts`:
     - `POST` -- requireOrgAdmin -> call existing `discoverMcpTools()` from `@/lib/mcp-client` -> update connection's availableTools -> return discovered tools
     - Reuse existing MCP discovery logic from `app/api/mcp/connections/[id]/discover/route.ts`

  4. Create `app/api/org/[slug]/admin/mcp/connections/[id]/test/route.ts`:
     - `POST` -- requireOrgAdmin -> test MCP connection -> return { success: boolean, error?: string }
     - Reuse existing MCP test logic from `app/api/mcp/connections/[id]/test/route.ts`

  **B. MCP Management UI:**

  5. Create `components/admin/mcp-assignment-panel.tsx`:
     - Shows list of MCP connections with assignment type badge (Org-wide / Role: {name})
     - "Add MCP Server" button opens a form dialog with: name, server URL, auth type (none/api_key/oauth), credentials, assignment type (org-wide or role-specific), role selector (if role-specific)
     - Per connection: status badge, test button, discover tools button, delete button
     - Reuse UI patterns from existing `components/mcp/mcp-add-dialog.tsx` and `components/mcp/mcp-connection-card.tsx` but adapted for admin context (assignment type selector, role dropdown)

  6. Create `app/org/[slug]/admin/mcp/page.tsx`:
     - "MCP Server Management" title + "Add MCP Server" button
     - McpAssignmentPanel component
     - Sections: "Org-wide Servers" (roleId = null) and "Role-specific Servers" (grouped by role name)
     - Fetch roles list for role selector dropdown
  </action>
  <verify>
    Navigate to `/org/{slug}/admin/mcp` -- shows MCP management page.
    Add an MCP server as org-wide -- appears in "Org-wide Servers" section.
    Add an MCP server as role-specific for Technical -- appears under Technical role section.
    Test and discover work on connections.
    Delete a connection -- removed from list.
    `npm run lint` passes.
  </verify>
  <done>
    Org Admin can connect MCP servers and assign them org-wide (OMCP-01, OMCP-02) or to specific roles (OMCP-03). Both assignment types coexist (OMCP-04). Org Admin can remove connections (OMCP-05).
  </done>
</task>

<task type="auto">
  <name>Task 2: User custom instructions API and Settings modal integration</name>
  <files>app/api/org/[slug]/user/custom-instructions/route.ts, components/settings-modal.tsx</files>
  <action>
  **A. User Custom Instructions API (UCUST-01 through UCUST-04):**

  1. Create `app/api/org/[slug]/user/custom-instructions/route.ts`:
     - `GET` -- requireOrgAuth -> return { customInstructions: orgMember.customInstructions, enabled: role.customInstructionsEnabled, maxTokens: TOKEN_LIMITS.user }
     - `PATCH` -- requireOrgAuth -> check role.customInstructionsEnabled (if false, return 403 "Custom instructions disabled for your role") -> validate token budget (200 tokens with SERVER_MARGIN) -> update orgMember.customInstructions via tenantDb -> return updated. UCUST-03 is automatic: customInstructions is on OrgMember which is org-specific.
     - Zod schema: `{ customInstructions: z.string().max(TOKEN_LIMITS.user * 4 * SERVER_MARGIN) }` with refine for token count check

  **B. Wire custom instructions into settings-modal.tsx (MANDATORY -- no standalone component):**

  2. Modify `components/settings-modal.tsx` to add a "Custom Instructions" section. This file is 42.7KB -- make ONLY the changes described below, do NOT refactor other parts.

     Specific changes:
     a. Import the `InstructionEditor` component from `@/components/admin/instruction-editor` (created in Plan 04).
     b. Add a new tab or section in the settings modal labeled "Custom Instructions" (or add it below the existing "Default Model" section if tabs are not used).
     c. On mount (or when the section is opened), fetch from `GET /api/org/${orgSlug}/custom-instructions`. The org slug is available from the component's context (check how the existing settings modal resolves org context -- likely via localStorage or URL params).
     d. Render the `InstructionEditor` component with:
        - `value`: the custom instructions text from the API response
        - `maxTokens`: 200 (from TOKEN_LIMITS.user)
        - `label`: "Custom Instructions"
        - `description`: "Personalize how the AI responds to you. These instructions are added to every conversation."
        - `disabled`: `!enabled` (from API response)
        - `disabledMessage`: "Custom instructions disabled by your admin." (UCUST-04)
     e. Add a "Save" button that calls `PATCH /api/org/${orgSlug}/user/custom-instructions` with the current text.
     f. Show success/error feedback on save (inline message or toast, following existing patterns in settings-modal.tsx).
     g. When `enabled` is false (UCUST-04, SAFE-08): The InstructionEditor renders with grayed-out textarea showing the saved text and the disabled message. The save button is hidden or disabled. The text remains visible but cannot be edited.

     This ensures UCUST-04 is satisfied: "the user's saved text is visible but grayed out with 'Custom instructions disabled by your admin' message."
  </action>
  <verify>
    GET /api/org/{slug}/user/custom-instructions returns user's custom instructions and enabled flag.
    PATCH with text > 200 tokens returns 400.
    PATCH when custom instructions are disabled for role returns 403.
    Open Settings modal as a user whose role has custom instructions enabled -- "Custom Instructions" section visible with InstructionEditor, save works.
    Open Settings modal as a user whose role has custom instructions DISABLED -- text visible but grayed out with "Custom instructions disabled by your admin" message.
    `npm run lint` passes.
  </verify>
  <done>
    User custom instructions API validates against 200 token limit and respects role-level enable/disable flag (UCUST-01, UCUST-02, UCUST-03). Custom instructions section wired into the existing settings modal (not a standalone component). When disabled, saved text is visible but grayed out with admin message (UCUST-04, SAFE-08).
  </done>
</task>

</tasks>

<verification>
- MCP management page shows org-wide and role-specific connections
- Add, test, discover, delete MCP connections work
- User custom instructions API validates token limit
- User custom instructions respect role enable/disable flag
- Custom instructions section accessible via Settings modal (not a standalone page)
- Disabled custom instructions show grayed-out text with admin message in Settings modal
- `npm run lint` passes
</verification>

<success_criteria>
- Org Admin can connect and assign MCP servers org-wide or role-specific (OMCP-01 through OMCP-05)
- User custom instructions validated at 200 tokens (UCUST-01, UCUST-02)
- Custom instructions are org-specific via OrgMember (UCUST-03)
- Disabled custom instructions visible but grayed out in Settings modal with admin message (UCUST-04, SAFE-08)
</success_criteria>

<output>
After completion, create `.planning/phases/03-chat-integration-and-core-rbac/03-06-SUMMARY.md`
</output>
