---
phase: 03-chat-integration-and-core-rbac
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/org/[slug]/admin/models/route.ts
  - components/admin/role-model-assignment.tsx
  - components/ui/toast.tsx
  - components/ui/alert-dialog.tsx
  - components/ui/confirmation-dialog.tsx
  - app/layout.tsx
  - package.json
autonomous: true
gap_closure: true
requirements: [OLLM-01, OLLM-02, MODL-05, MODL-07, OMCP-05]

must_haves:
  truths:
    - "Org Admin can load model assignment page without 403 error"
    - "Toast notifications appear for save/delete/error operations across admin console"
    - "Styled confirmation dialog replaces native browser confirm() everywhere"
    - "Org-scoped models endpoint returns active models for org admin users"
  artifacts:
    - path: "app/api/org/[slug]/admin/models/route.ts"
      provides: "Org-scoped models endpoint for org admins"
      exports: ["GET"]
    - path: "components/ui/toast.tsx"
      provides: "Toast notification system using sonner"
      exports: ["Toaster", "toast"]
    - path: "components/ui/alert-dialog.tsx"
      provides: "Radix AlertDialog wrapper component"
      exports: ["AlertDialog", "AlertDialogTrigger", "AlertDialogContent", "AlertDialogAction", "AlertDialogCancel"]
    - path: "components/ui/confirmation-dialog.tsx"
      provides: "Reusable confirmation dialog for destructive actions"
      exports: ["ConfirmationDialog"]
  key_links:
    - from: "components/admin/role-model-assignment.tsx"
      to: "app/api/org/[slug]/admin/models/route.ts"
      via: "fetch in useEffect"
      pattern: "fetch.*api/org.*admin/models"
    - from: "app/layout.tsx"
      to: "components/ui/toast.tsx"
      via: "Toaster component rendered in root layout"
      pattern: "<Toaster"
---

<objective>
Fix the BLOCKER model endpoint bug and establish shared UI infrastructure (toast notifications, styled confirmation dialogs) needed by all subsequent gap closure plans.

Purpose: The role model assignment page is completely broken because it calls the super-admin-only `/api/admin/models` endpoint instead of an org-scoped one. This plan fixes that blocker and also installs the two foundational UI components (toast system, confirmation dialog) that every other gap closure plan depends on.

Output: Working model assignment page, sonner toast system, Radix AlertDialog component, reusable ConfirmationDialog component.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chat-integration-and-core-rbac/03-retest-UAT.md

Key understanding:
- `components/admin/role-model-assignment.tsx` line 59 fetches `/api/admin/models?status=ACTIVE` which requires `requireSuperAdmin()` — org admins get 403
- Need to create `GET /api/org/[slug]/admin/models` endpoint using `requireOrgAdmin()` that returns all active models from the Model Registry
- No toast library is currently installed — need to add `sonner` package
- No `@radix-ui/react-alert-dialog` installed — need to add it
- Existing `components/ui/tooltip.tsx` already uses Radix pattern — follow same style for AlertDialog
- `app/layout.tsx` is root layout where Toaster provider goes
- The MCP panel, instructions page, and roles page all need toast + confirmation dialogs (addressed in subsequent plans)

<interfaces>
From lib/auth-middleware.ts:
```typescript
export async function requireOrgAdmin(request: Request): Promise<OrgAuthContext>
// Returns: { user, orgMember, role, permissions, tenantDb, organizationId }
```

From lib/services/model-registry-service.ts:
```typescript
export async function getAllModels(filters?: { status?: string }): Promise<Model[]>
```

From app/api/org/[slug]/models/route.ts (existing user-facing endpoint):
```typescript
// GET handler returns models for the user's role's allowedModels
// The admin endpoint should return ALL active models (not role-filtered)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sonner + @radix-ui/react-alert-dialog and create shared UI components</name>
  <files>
    package.json
    components/ui/toast.tsx
    components/ui/alert-dialog.tsx
    components/ui/confirmation-dialog.tsx
    app/layout.tsx
  </files>
  <action>
1. Install packages:
   ```bash
   npm install sonner @radix-ui/react-alert-dialog
   ```

2. Create `components/ui/toast.tsx`:
   - Export a `Toaster` component wrapping sonner's `<Toaster>` with project styling:
     - `position="bottom-right"`
     - `richColors` enabled
     - `closeButton` enabled
     - `duration={4000}` (4 seconds default)
     - Theme-aware: use `theme` prop bound to document class (detect `.dark`)
   - Re-export `toast` function from sonner for easy imports: `import { toast } from "@/components/ui/toast"`

3. Create `components/ui/alert-dialog.tsx`:
   - Follow the exact same pattern as the existing `components/ui/dialog.tsx` (Radix primitive wrapper with shadcn styling)
   - Export: `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogAction`, `AlertDialogCancel`
   - `AlertDialogAction` should use destructive button styling by default (red/coral background)
   - `AlertDialogCancel` should use outline/ghost styling
   - Use `cn()` utility for class merging

4. Create `components/ui/confirmation-dialog.tsx`:
   - A higher-level reusable wrapper around AlertDialog for destructive actions
   - Props interface:
     ```typescript
     interface ConfirmationDialogProps {
       open: boolean
       onOpenChange: (open: boolean) => void
       title: string
       description: string
       confirmLabel?: string  // default: "Delete"
       cancelLabel?: string   // default: "Cancel"
       variant?: "destructive" | "warning"  // default: "destructive"
       onConfirm: () => void | Promise<void>
       loading?: boolean
     }
     ```
   - Renders AlertDialog with title, description, cancel button, and confirm button
   - Confirm button shows loading spinner when `loading` is true
   - destructive variant: red/coral confirm button. warning variant: amber/orange confirm button

5. Add `<Toaster />` to `app/layout.tsx`:
   - Import `Toaster` from `@/components/ui/toast`
   - Place `<Toaster />` as a sibling of `{children}` inside the `<body>` tag
   - This makes toasts available globally without any provider wiring in individual pages
  </action>
  <verify>
    <automated>
    npm run build 2>&1 | tail -20
    # Should compile without errors. Verify the new components exist and export correctly.
    </automated>
  </verify>
  <done>
  - sonner and @radix-ui/react-alert-dialog packages installed
  - Toast component created with project-consistent styling
  - AlertDialog component created following existing Radix wrapper pattern
  - ConfirmationDialog created as high-level reusable wrapper
  - Toaster rendered in root layout — toasts work globally
  - `import { toast } from "@/components/ui/toast"` works from any component
  - `import { ConfirmationDialog } from "@/components/ui/confirmation-dialog"` works from any component
  </done>
</task>

<task type="auto">
  <name>Task 2: Create org-scoped admin models endpoint and fix role model assignment component</name>
  <files>
    app/api/org/[slug]/admin/models/route.ts
    components/admin/role-model-assignment.tsx
  </files>
  <action>
1. Create `app/api/org/[slug]/admin/models/route.ts`:
   - GET handler:
     - Call `requireOrgAdmin(request)` for auth
     - Call `getAllModels({ status: 'ACTIVE' })` from `lib/services/model-registry-service`
     - Return JSON array of active models (same shape as `/api/admin/models` but org-admin accessible)
     - No need to filter by role — this is for the admin assignment UI, which needs ALL active models
   - Do NOT add POST/PATCH/DELETE — model CRUD stays in super-admin routes

2. Update `components/admin/role-model-assignment.tsx`:
   - Change the fetch URL on line 59 from `/api/admin/models?status=ACTIVE` to `/api/org/${orgSlug}/admin/models`
   - The component needs to receive `orgSlug` as a prop. Update the `RoleModelAssignmentProps` interface to include `orgSlug: string`
   - Find all parents that render `<RoleModelAssignment>` (which is `app/org/[slug]/admin/roles/page.tsx`) and pass `orgSlug` prop
   - Add error state styling: replace the plain red text error message with a styled alert card:
     - Use Card component with red-tinted border
     - AlertCircle icon (already imported from lucide-react in the file)
     - Error message text
     - "Retry" button that calls fetchModels() again
   - Add import for `toast` from `@/components/ui/toast`
   - Add toast.success("Models saved for {roleName}") after successful model save
   - Add toast.error("Failed to save models") on save error
  </action>
  <verify>
    <automated>
    npm run build 2>&1 | tail -20
    # Should compile without errors. The org admin models endpoint and updated component should build correctly.
    </automated>
  </verify>
  <done>
  - GET /api/org/[slug]/admin/models endpoint exists with requireOrgAdmin auth
  - RoleModelAssignment component fetches from org-scoped endpoint instead of super-admin endpoint
  - Org admins can load the model assignment page without 403 errors
  - Error state shows styled alert card with retry button instead of plain red text
  - Toast notifications appear on model save success/error
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build` completes without errors
2. New endpoint accessible: Org admin user can GET /api/org/{slug}/admin/models and receive active models list
3. Role model assignment page loads models correctly (no 403)
4. Toast notifications visible when saving model assignments
5. AlertDialog and ConfirmationDialog components render correctly
</verification>

<success_criteria>
- BLOCKER resolved: Role model assignment page loads models for org admins
- Toast system available globally via `import { toast } from "@/components/ui/toast"`
- ConfirmationDialog available via `import { ConfirmationDialog } from "@/components/ui/confirmation-dialog"`
- All new components follow existing codebase patterns (Radix UI wrappers, cn() utility, CVA variants)
</success_criteria>

<output>
After completion, create `.planning/phases/03-chat-integration-and-core-rbac/03-08-SUMMARY.md`
</output>
