---
phase: 03-chat-integration-and-core-rbac
plan: 11
type: execute
wave: 2
depends_on: [03-08]
files_modified:
  - components/admin/admin-sidebar.tsx
  - components/admin/admin-breadcrumb.tsx
  - app/org/[slug]/admin/page.tsx
  - app/org/[slug]/admin/layout.tsx
  - app/org/[slug]/admin/instructions/page.tsx
  - app/org/[slug]/admin/roles/page.tsx
  - app/org/[slug]/admin/mcp/page.tsx
  - components/ui/skeleton-loaders.tsx
autonomous: true
gap_closure: true
requirements: [UCHAT-01, UCHAT-05, UCHAT-06]

must_haves:
  truths:
    - "Non-admin users see a toast notification explaining why they were redirected from admin console"
    - "Admin console pages show loading skeletons during initial data fetch"
    - "Breadcrumb navigation shows current location in admin console hierarchy"
    - "Admin root /admin redirects to a dashboard overview page (not straight to instructions)"
    - "Coming Soon badges use muted/subtle styling instead of prominent badges"
    - "Sign Out button has destructive styling distinct from Back to Chat"
  artifacts:
    - path: "components/admin/admin-breadcrumb.tsx"
      provides: "Breadcrumb navigation component for admin console pages"
      exports: ["AdminBreadcrumb"]
    - path: "app/org/[slug]/admin/page.tsx"
      provides: "Admin dashboard overview landing page"
    - path: "components/ui/skeleton-loaders.tsx"
      provides: "Admin-specific loading skeleton components"
      contains: "AdminInstructionsSkeleton"
  key_links:
    - from: "app/org/[slug]/admin/layout.tsx"
      to: "components/admin/admin-breadcrumb.tsx"
      via: "rendered in layout header area"
      pattern: "AdminBreadcrumb"
    - from: "app/org/[slug]/admin/layout.tsx"
      to: "components/ui/toast.tsx"
      via: "toast on redirect for non-admin users"
      pattern: "toast\\.error"
---

<objective>
Add breadcrumb navigation, admin dashboard landing page, loading skeletons, redirect notification, and sidebar visual polish to the Org Admin Console.

Purpose: The admin console lacks standard SaaS navigation patterns (breadcrumbs, dashboard landing page), provides no feedback when non-admins are redirected, shows blank content during 3-4 second data loads, and has visual inconsistencies in the sidebar (Coming Soon badges too prominent, Sign Out same style as Back to Chat).

Output: Polished admin console navigation with breadcrumbs, overview dashboard, loading skeletons, redirect toast, and refined sidebar.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key understanding:
- `components/admin/admin-sidebar.tsx` (207 lines) has org-admin variant with nav items + Coming Soon badges
- `app/org/[slug]/admin/layout.tsx` (131 lines) handles auth check, redirects non-admins to chat
- `app/org/[slug]/admin/page.tsx` currently exists — need to check if it redirects to /instructions or serves as landing
- Non-admin redirect happens silently — user sees chat page with no explanation
- Instructions page takes 3-4 sec to load with blank content area
- `components/ui/skeleton-loaders.tsx` exists with ConversationListSkeleton and other chat skeletons
- "Coming Soon" badges use default Badge component — too prominent
- Sign Out and Back to Chat use same button styling — risk of accidental sign-out

<interfaces>
From components/ui/toast.tsx (created in 03-08):
```typescript
export { toast } from "sonner"
```

From components/admin/admin-sidebar.tsx (existing):
```typescript
interface NavItem {
  label: string
  icon: React.ElementType
  href: string
  enabled: boolean
}
function getOrgAdminNavItems(orgSlug: string): NavItem[]
// Returns array with enabled items (Instructions, Role Settings, MCP Servers)
// and disabled items (Users, Settings, Analytics, Audit Logs) marked with "Coming Soon"
```

From components/ui/skeleton-loaders.tsx (existing):
```typescript
export function ConversationListSkeleton({ count }: { count?: number }): JSX.Element
export function PageLoadingSkeleton(): JSX.Element
// Need to add admin-specific skeletons
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create breadcrumb component and admin dashboard landing page</name>
  <files>
    components/admin/admin-breadcrumb.tsx
    app/org/[slug]/admin/page.tsx
  </files>
  <action>
1. **Create `components/admin/admin-breadcrumb.tsx`:**
   - A simple breadcrumb component that reads the current pathname and renders a breadcrumb trail
   - Props: `orgSlug: string` (to build correct paths)
   - Logic: Parse `usePathname()` to determine current page
   - Breadcrumb segments:
     - Always show "Admin Console" as the first item (links to `/org/{slug}/admin`)
     - Map path segments to labels:
       - `/instructions` → "System Instructions"
       - `/roles` → "Role Settings"
       - `/mcp` → "MCP Servers"
     - Current page (last segment) is rendered as plain text (not a link)
     - Separator: ChevronRight icon (from lucide-react)
   - Styling: `text-sm text-muted-foreground` with `hover:text-foreground` on links
   - Return null if at the admin root (no breadcrumb needed on dashboard)

2. **Update `app/org/[slug]/admin/page.tsx` to be a dashboard overview:**
   - Instead of redirecting to `/instructions`, render a dashboard overview page
   - Content (all placeholder/static for now — will be populated in Phase 6):
     - **Welcome header:** "Welcome to {orgName} Admin Console" with subtitle "Manage your organization's AI chat settings"
     - **Quick links grid** (3 cards in a responsive grid):
       - "System Instructions" card — icon (MessageSquare), brief description "Configure org-wide and role-specific AI instructions", link to /instructions
       - "Role Settings" card — icon (Users), brief description "Manage model access and role permissions", link to /roles
       - "MCP Servers" card — icon (Plug), brief description "Connect and manage MCP tool servers", link to /mcp
     - Each card uses the Card component with hover effect (`hover:border-primary/50 transition-colors`)
     - Cards show the section icon, title, description, and a subtle arrow icon
   - Use existing Card, CardHeader, CardTitle, CardContent from `@/components/ui/card`
   - Import icons from lucide-react matching the sidebar nav items
  </action>
  <verify>
    <automated>
    npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <done>
  - AdminBreadcrumb component renders trail: "Admin Console > System Instructions"
  - Admin root (/admin) shows dashboard overview with quick link cards
  - Dashboard has welcome header and 3 section cards with descriptions
  - Breadcrumb returns null on dashboard root page
  </done>
</task>

<task type="auto">
  <name>Task 2: Add loading skeletons, redirect toast, and sidebar visual polish</name>
  <files>
    app/org/[slug]/admin/layout.tsx
    app/org/[slug]/admin/instructions/page.tsx
    app/org/[slug]/admin/roles/page.tsx
    app/org/[slug]/admin/mcp/page.tsx
    components/admin/admin-sidebar.tsx
    components/ui/skeleton-loaders.tsx
  </files>
  <action>
1. **Add admin-specific loading skeletons to `components/ui/skeleton-loaders.tsx`:**
   - `AdminInstructionsSkeleton`: Mimics the instruction editors layout
     - Title skeleton bar (200px wide)
     - Textarea skeleton (full width, 120px height)
     - Token counter skeleton bar (100px wide)
     - Progress bar skeleton (full width, 4px height)
     - Save button skeleton (80px wide)
     - Repeat 3x for role sections with separator between
   - `AdminRoleCardsSkeleton`: Mimics role cards layout
     - 3 card skeletons with header bar, checkbox rows (4 rows), and toggle rows (2 rows)
   - `AdminMcpSkeleton`: Mimics MCP panel layout
     - Section header skeleton
     - Empty state box or 2 card skeletons with icon, text lines, and action buttons

2. **Add loading skeletons to admin pages:**
   - `app/org/[slug]/admin/instructions/page.tsx`:
     - Show `AdminInstructionsSkeleton` while data is loading (before org instructions and roles have loaded)
     - Replace the current blank loading state
   - `app/org/[slug]/admin/roles/page.tsx`:
     - Show `AdminRoleCardsSkeleton` while roles are loading
   - `app/org/[slug]/admin/mcp/page.tsx`:
     - Show `AdminMcpSkeleton` while connections are loading

3. **Non-admin redirect notification in `app/org/[slug]/admin/layout.tsx`:**
   - Import `toast` from `@/components/ui/toast`
   - When the layout detects the user is not an org admin and redirects to chat:
     - Before `router.push()`, call `toast.error("Access denied. You don't have admin privileges for this organization.")`
     - The toast will persist across navigation because it's rendered in the root layout's Toaster

4. **Add breadcrumb to admin layout:**
   - Import `AdminBreadcrumb` from `@/components/admin/admin-breadcrumb`
   - Render it in the `SidebarInset` area, above `{children}`, as a header bar
   - Wrap in a `<div className="border-b px-6 py-3">` for consistent page header styling

5. **Sidebar visual polish in `components/admin/admin-sidebar.tsx`:**
   - **Coming Soon badges:** Change from default Badge styling to a more muted style:
     - Use `variant="outline"` (if available) or custom class: `text-xs text-muted-foreground/60 border-muted-foreground/20 bg-transparent`
     - Reduce font size to `text-[10px]` or `text-xs`
     - Lower opacity to make them recede visually
   - **Sign Out button destructive styling:**
     - Change Sign Out button from the same style as "Back to Chat" to use destructive/danger styling
     - Use `variant="ghost"` with `text-destructive hover:text-destructive hover:bg-destructive/10`
     - Keep the LogOut icon
   - **Gear icon clipping fix (chat sidebar):**
     - This is the Admin Console button in the chat sidebar (in `full-chat-app.tsx`), not the admin sidebar
     - Add `pl-1` or `ml-1` padding to the gear icon button to prevent viewport clipping
     - NOTE: Only add this fix if the gear icon is in `admin-sidebar.tsx` — if it's in `full-chat-app.tsx`, note it for a separate fix but don't modify full-chat-app.tsx in this plan
  </action>
  <verify>
    <automated>
    npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <done>
  - Loading skeletons show on Instructions, Roles, and MCP pages during data fetch
  - Non-admin users see toast "Access denied" message when redirected from admin
  - Breadcrumb navigation visible on all admin sub-pages (not on dashboard root)
  - Coming Soon badges are muted and subtle
  - Sign Out button has destructive red styling
  - No blank content areas during page loads
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build`
2. Navigate to /admin root — see dashboard overview with quick link cards
3. Navigate to /admin/instructions — breadcrumb shows "Admin Console > System Instructions"
4. Page loads show skeletons instead of blank space
5. Non-admin redirect shows toast notification
6. Coming Soon badges are subtler than before
7. Sign Out button is visually distinct from Back to Chat
</verification>

<success_criteria>
- Admin console has proper navigation hierarchy (dashboard > section pages)
- Breadcrumb shows current location at all times
- Loading states use content-shaped skeletons matching final layout
- Non-admin redirect is not silent — user understands why they were redirected
- Sidebar visual polish: muted Coming Soon, destructive Sign Out
</success_criteria>

<output>
After completion, create `.planning/phases/03-chat-integration-and-core-rbac/03-11-SUMMARY.md`
</output>
