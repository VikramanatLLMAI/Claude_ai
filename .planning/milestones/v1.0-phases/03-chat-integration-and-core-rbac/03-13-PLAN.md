---
phase: 03-chat-integration-and-core-rbac
plan: 13
type: execute
wave: 2
depends_on: [03-08]
files_modified:
  - components/full-chat-app.tsx
  - components/admin/mcp-assignment-panel.tsx
  - app/org/[slug]/admin/mcp/page.tsx
autonomous: true
gap_closure: true
requirements: [UCUST-01, UCUST-02, UCUST-03, UCUST-04, MODL-01, MODL-02, MODL-03, MODL-04, MODL-06]

must_haves:
  truths:
    - "Gear icon in chat sidebar is not clipped by viewport edge"
    - "MCP empty states show descriptive illustration-style layout with CTA button"
    - "Model fetch error in role assignment shows styled alert with retry button"
  artifacts:
    - path: "components/full-chat-app.tsx"
      provides: "Chat app with fixed gear icon clipping"
      contains: "Admin Console"
    - path: "components/admin/mcp-assignment-panel.tsx"
      provides: "MCP panel with enhanced empty states"
      contains: "empty-state"
  key_links:
    - from: "components/full-chat-app.tsx"
      to: "components/admin/admin-sidebar.tsx"
      via: "Admin Console navigation from chat sidebar"
      pattern: "Admin Console"
---

<objective>
Fix the gear icon clipping in the chat sidebar, enhance MCP empty states with illustrations and CTAs, and address remaining low-priority visual polish items.

Purpose: These are the remaining visual polish items from the UAT audit: the gear icon clipping (#21), empty states lacking CTAs (#15 for MCP), and the model fetch error styling (additional functional issue). Small individually but collectively they elevate the admin console from a functional tool to a polished SaaS product.

Output: Fixed gear icon, enhanced MCP empty states, styled error states.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key understanding:
- Gear icon for Admin Console button in chat sidebar is slightly clipped by viewport edge (item #21)
- The gear icon is in `components/full-chat-app.tsx` in the sidebar footer area
- MCP empty states (both org-wide and role-specific sections when 0 servers) show only plain text
- Model fetch error in role-model-assignment is plain red text — needs styled alert card (this may already be partially fixed in 03-08 Task 2, but verify/enhance)
- The full-chat-app.tsx is a large file (86KB) — need to make a targeted, minimal change

<interfaces>
From components/full-chat-app.tsx (existing):
```typescript
// Around line 583: Admin Console button in sidebar footer
// Conditionally rendered when isOrgAdmin && orgSlug
// Contains a gear icon (Settings or similar) + "Admin Console" text
```

From components/admin/mcp-assignment-panel.tsx (existing, enhanced in 03-09):
```typescript
// Has org-wide and role-specific sections
// Empty state renders when connections.length === 0 for each section
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix gear icon clipping and enhance empty states</name>
  <files>
    components/full-chat-app.tsx
    components/admin/mcp-assignment-panel.tsx
    app/org/[slug]/admin/mcp/page.tsx
  </files>
  <action>
1. **Fix gear icon clipping in chat sidebar (`components/full-chat-app.tsx`):**
   - Find the Admin Console button in the sidebar footer (around line 583)
   - The gear icon is getting clipped by the sidebar's overflow or edge
   - Add left padding/margin to the button or its container: `pl-1` or `ml-1`
   - Alternatively, the issue might be the parent container's `overflow-hidden` — check if the button's parent has overflow clipping and add appropriate padding
   - This should be a 1-2 line change — do NOT restructure the sidebar layout
   - Keep the change minimal since this is an 86KB file

2. **Enhanced MCP empty states (`components/admin/mcp-assignment-panel.tsx`):**
   - Replace the plain text empty states for both org-wide and role-specific sections with a structured empty state layout:
   - **Org-wide empty state:**
     - Icon: Globe (from lucide-react) in a large muted circle (48px circle with bg-muted, icon 24px)
     - Title: "No org-wide MCP servers"
     - Description: "MCP servers connected here are available to all users in your organization. Connect a server to give your team access to external tools."
     - CTA Button: "Add Org-Wide Server" (primary variant) — opens the Add dialog with org-wide pre-selected
   - **Role-specific empty state:**
     - Icon: Shield (from lucide-react) in a large muted circle
     - Title: "No role-specific MCP servers"
     - Description: "MCP servers connected here are only available to users with specific roles. Assign servers to control which roles can access which tools."
     - CTA Button: "Add Role-Specific Server" (outline variant) — opens the Add dialog with role-specific pre-selected
   - Layout: centered, vertical stack, `py-8` padding for breathing room
   - The CTA buttons should call `setShowAddDialog(true)` with appropriate pre-selection of assignment type

3. **MCP page empty state (`app/org/[slug]/admin/mcp/page.tsx`):**
   - If the MCP page itself has a top-level empty state (no connections at all):
     - Show a unified empty state with both options:
       - Icon: Plug (from lucide-react) in a large circle
       - Title: "Connect your first MCP server"
       - Description: "MCP (Model Context Protocol) servers extend your AI assistant's capabilities with external tools. Connect servers to give your team access to databases, APIs, and custom tooling."
       - Two CTA buttons: "Add Org-Wide Server" and "Add Role-Specific Server"
   - This only shows when total connections (org-wide + role-specific) is 0
  </action>
  <verify>
    <automated>
    npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <done>
  - Gear icon in chat sidebar has proper spacing and is not clipped
  - MCP org-wide empty state shows icon, description, and CTA button
  - MCP role-specific empty state shows icon, description, and CTA button
  - Top-level MCP empty state shows when no connections exist at all
  - CTA buttons open the Add dialog with correct assignment type pre-selected
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build`
2. Chat sidebar gear icon fully visible with no clipping
3. MCP page with 0 connections shows comprehensive empty state with CTAs
4. Each section's empty state is contextually appropriate
</verification>

<success_criteria>
- Gear icon clipping is fixed (item #21)
- Empty states have descriptive text, icons, and actionable CTA buttons (item #15 for MCP)
- Changes to full-chat-app.tsx are minimal (1-2 lines)
- Empty states guide new admins toward taking action
</success_criteria>

<output>
After completion, create `.planning/phases/03-chat-integration-and-core-rbac/03-13-SUMMARY.md`
</output>
