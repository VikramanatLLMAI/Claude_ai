---
phase: 05-super-admin-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - app/super-admin/organizations/page.tsx
  - app/super-admin/super-admins/page.tsx
  - components/admin/admin-sidebar.tsx
autonomous: true
requirements:
  - SUI-01
  - SUI-03

must_haves:
  truths:
    - "Super Admin can view all organizations in a TanStack Table with sorting, filtering, and pagination"
    - "Super Admin can create, edit, suspend, activate, and delete organizations via row actions and modals"
    - "Super Admin can view all Super Admins in a TanStack Table"
    - "Super Admin can create, edit, and delete Super Admins via row actions and modals"
    - "Sidebar uses grouped sections (Management, Monitoring, Configuration) with all items enabled"
  artifacts:
    - path: "app/super-admin/organizations/page.tsx"
      provides: "Organizations management page with DataTable and CRUD modals"
    - path: "app/super-admin/super-admins/page.tsx"
      provides: "Super Admins management page with DataTable and CRUD modals"
    - path: "components/admin/admin-sidebar.tsx"
      provides: "Updated sidebar with grouped navigation"
  key_links:
    - from: "app/super-admin/organizations/page.tsx"
      to: "/api/super-admin/organizations"
      via: "fetch calls for CRUD operations"
    - from: "app/super-admin/super-admins/page.tsx"
      to: "/api/super-admin/super-admins"
      via: "fetch calls for CRUD operations"
---

<objective>
Build the Organizations and Super Admins management pages using the DataTable component, and upgrade the sidebar to grouped navigation.

Purpose: These are the two core management pages that Super Admin uses most. Together with the sidebar upgrade, this transforms the admin panel from a single-page (Models) shell into a multi-section management tool.

Output: Two fully functional management pages + polished grouped sidebar navigation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-super-admin-dashboard/05-CONTEXT.md
@.planning/phases/05-super-admin-dashboard/05-RESEARCH.md
@.planning/phases/05-super-admin-dashboard/05-01-SUMMARY.md
@.planning/phases/05-super-admin-dashboard/05-02-SUMMARY.md

@components/admin/data-table.tsx
@components/admin/admin-sidebar.tsx
@components/admin/admin-breadcrumb.tsx
@components/ui/dialog.tsx
@components/ui/dropdown-menu.tsx
@components/ui/badge.tsx
@components/ui/toast.tsx
@components/ui/confirmation-dialog.tsx
@lib/services/org-service.ts
@lib/services/super-admin-service.ts
@lib/validation.ts
@app/api/super-admin/organizations/route.ts
@app/api/super-admin/super-admins/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade admin sidebar to grouped navigation</name>
  <files>components/admin/admin-sidebar.tsx</files>
  <action>
Refactor the `SUPER_ADMIN_NAV_ITEMS` flat array to use the `NavGroup[]` structure (already defined in the file for org admin).

Replace the flat `SUPER_ADMIN_NAV_ITEMS` with grouped navigation:

```typescript
const SUPER_ADMIN_NAV_GROUPS: NavGroup[] = [
  {
    label: "Management",
    items: [
      { label: "Models", icon: Cpu, href: "/super-admin/models", enabled: true },
      { label: "Organizations", icon: Building2, href: "/super-admin/organizations", enabled: true },
      { label: "Super Admins", icon: Shield, href: "/super-admin/super-admins", enabled: true },
      { label: "API Keys", icon: Key, href: "/super-admin/api-keys", enabled: true },
    ],
  },
  {
    label: "Monitoring",
    items: [
      { label: "Analytics", icon: BarChart3, href: "/super-admin/analytics", enabled: true },
      { label: "Audit Logs", icon: FileText, href: "/super-admin/audit-logs", enabled: true },
    ],
  },
  {
    label: "Configuration",
    items: [
      { label: "Settings", icon: Settings, href: "/super-admin/settings", enabled: true },
      { label: "System Prompt", icon: MessageSquare, href: "/super-admin/system-prompt", enabled: true },
    ],
  },
]
```

All items `enabled: true` -- remove the "Coming Soon" badge logic for super admin variant. The rendering block for `variant === "super-admin"` should use the same `SidebarGroup` + `SidebarGroupLabel` pattern already used by the org admin variant.

Add `MessageSquare` icon import (for System Prompt item) if not already imported.

Keep the org admin variant completely unchanged.
  </action>
  <verify>
    <automated>cd /c/Python_project/Antigravity_folder/chatbot_ui && grep -c "SidebarGroupLabel" components/admin/admin-sidebar.tsx && grep "Coming Soon" components/admin/admin-sidebar.tsx | grep -v "org-admin" | wc -l</automated>
  </verify>
  <done>Sidebar super-admin variant uses grouped sections (Management, Monitoring, Configuration). All 8 items enabled. No "Coming Soon" badges in super admin section. Org admin variant unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Organizations management page</name>
  <files>app/super-admin/organizations/page.tsx</files>
  <action>
Create the Organizations management page at `app/super-admin/organizations/page.tsx`.

**Page structure (consistent header pattern from CONTEXT.md):**
- Breadcrumb trail using `AdminBreadcrumb`
- Header row: "Organizations" h1 title + "Create Organization" primary Button at top-right
- DataTable with organization data below

**Column definitions (ColumnDef<Organization>[]):**
1. `name` — Sortable, filterable. Cell shows org name with small slug text below in muted color
2. `status` — Sortable, filterable. Badge: green "Active", amber "Suspended", red "Deleted"
3. `_count.orgMembers` — "Users" header. Numeric count
4. `createdAt` — "Created" header. Formatted date (use `new Date(val).toLocaleDateString()`)
5. `actions` — Three-dot DropdownMenu with: Edit, Suspend/Activate (conditional), Delete. Uses `MoreVertical` icon from lucide-react

**CRUD modals:**
- **Create/Edit Dialog**: Form with fields: name (required), slug (auto-generated from name, editable), monthlyRequestCeiling (optional number), monthlyTokenCeiling (optional number). Uses existing Dialog, Input, Label, Button components. On submit: POST /api/super-admin/organizations (create) or PATCH /api/super-admin/organizations/[id] (edit)
- **Suspend confirmation**: Use ConfirmationDialog. POST /api/super-admin/organizations/[id]/suspend
- **Activate**: POST /api/super-admin/organizations/[id]/activate
- **Delete confirmation**: Use ConfirmationDialog with destructive variant. DELETE /api/super-admin/organizations/[id]

**Data fetching:**
- `useEffect` on mount: `GET /api/super-admin/organizations` with Bearer token from `getAuthHeaders()`
- After any mutation, refetch the list
- Loading state: show skeleton (or DataTable with empty data)
- Error state: toast.error()

**Safety rules enforced in UI:**
- Deleted orgs show "Restore" action instead of Edit/Suspend/Delete (calls POST /api/super-admin/organizations/[id]/restore)
- Suspended orgs show "Activate" instead of "Suspend"

All API calls use `getAuthHeaders()` pattern from existing admin pages. Show toast.success() on mutations.
  </action>
  <verify>
    <automated>cd /c/Python_project/Antigravity_folder/chatbot_ui && ls app/super-admin/organizations/page.tsx && grep -c "DataTable" app/super-admin/organizations/page.tsx && grep -c "DropdownMenu" app/super-admin/organizations/page.tsx</automated>
  </verify>
  <done>Organizations page renders a DataTable with all columns, sorting, filtering, pagination. CRUD modals work for create/edit/suspend/activate/delete/restore. Three-dot row actions menu present on each row.</done>
</task>

<task type="auto">
  <name>Task 3: Super Admins management page</name>
  <files>app/super-admin/super-admins/page.tsx</files>
  <action>
Create the Super Admins management page at `app/super-admin/super-admins/page.tsx`.

**Page structure:**
- Breadcrumb trail using AdminBreadcrumb
- Header row: "Super Admins" h1 title + "Create Super Admin" primary Button at top-right
- DataTable with super admin data below

**Column definitions (ColumnDef<SuperAdmin>[]):**
1. `name` — Sortable, filterable. Show name with email below in muted text
2. `email` — Sortable, filterable
3. `createdAt` — "Created" header. Formatted date
4. `actions` — Three-dot DropdownMenu: Edit, Delete

**CRUD modals:**
- **Create Dialog**: Form with name (required), email (required, email format), password (required, min 8). POST /api/super-admin/super-admins
- **Edit Dialog**: Form with name (editable), email (read-only, grayed out). PATCH /api/super-admin/super-admins/[id]
- **Delete confirmation**: ConfirmationDialog with destructive variant. DELETE /api/super-admin/super-admins/[id]

**Safety rules (SAFE-01, SAFE-06):**
- Current user's row: Delete action disabled with tooltip "Cannot delete yourself"
- If only 1 super admin exists: Delete action disabled with tooltip "Must have at least 1 Super Admin"
- Detect current user ID from localStorage session to compare

**Data fetching:** Same pattern as Organizations page — useEffect on mount, refetch after mutations, getAuthHeaders(), toast feedback.
  </action>
  <verify>
    <automated>cd /c/Python_project/Antigravity_folder/chatbot_ui && ls app/super-admin/super-admins/page.tsx && grep -c "DataTable" app/super-admin/super-admins/page.tsx && grep "SAFE" app/super-admin/super-admins/page.tsx | head -3</automated>
  </verify>
  <done>Super Admins page renders a DataTable with columns, sorting, filtering, pagination. Create/edit/delete modals work. Safety rules prevent self-deletion and last-SA deletion.</done>
</task>

</tasks>

<verification>
1. Sidebar shows 3 groups (Management, Monitoring, Configuration) for super-admin variant
2. All 8 sidebar items enabled and link to /super-admin/* paths
3. Organizations page loads with DataTable showing orgs from API
4. Super Admins page loads with DataTable showing SAs from API
5. CRUD operations (create, edit, delete, suspend, activate, restore) work via modals
6. Toast notifications appear on success/error
7. Safety rules prevent self-deletion and last-SA deletion in UI
</verification>

<success_criteria>
- Two functional management pages with full CRUD via DataTable
- Sidebar upgraded to grouped sections with all items enabled
- All data operations use existing API routes (now at /api/super-admin/*)
- Safety rules visually enforced (disabled actions with tooltips)
</success_criteria>

<output>
After completion, create `.planning/phases/05-super-admin-dashboard/05-03-SUMMARY.md`
</output>
