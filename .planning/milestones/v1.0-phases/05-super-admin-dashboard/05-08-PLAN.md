---
phase: 05-super-admin-dashboard
plan: 08
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07"]
files_modified:
  - app/super-admin/models/page.tsx
  - components/admin/model-registry-table.tsx
autonomous: false
requirements:
  - SUI-04

must_haves:
  truths:
    - "Model Registry table uses TanStack Table with sorting, filtering, and pagination"
    - "Generation grouping (Claude 4.6, 4.5, 4) is preserved in the upgraded table"
    - "All Super Admin pages are accessible, functional, and visually consistent"
    - "Build compiles without errors"
  artifacts:
    - path: "app/super-admin/models/page.tsx"
      provides: "Model Registry page using TanStack Table"
    - path: "components/admin/model-registry-table.tsx"
      provides: "Upgraded model registry table with TanStack Table"
  key_links:
    - from: "components/admin/model-registry-table.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable with generation-grouped row data"
---

<objective>
Upgrade the Model Registry table to TanStack Table (preserving generation grouping) and perform final verification of all Phase 5 deliverables.

Purpose: The Model Registry is the only existing table not yet upgraded to TanStack Table. This plan completes the SUI-02 requirement for consistent tables. The verification checkpoint ensures all Phase 5 work is polished and functional.

Output: Upgraded Model Registry + human-verified complete Phase 5.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-super-admin-dashboard/05-CONTEXT.md
@.planning/phases/05-super-admin-dashboard/05-01-SUMMARY.md
@.planning/phases/05-super-admin-dashboard/05-02-SUMMARY.md
@.planning/phases/05-super-admin-dashboard/05-03-SUMMARY.md

@components/admin/model-registry-table.tsx
@components/admin/model-registry-form.tsx
@app/super-admin/models/page.tsx
@components/admin/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Model Registry table to TanStack Table</name>
  <files>
    components/admin/model-registry-table.tsx,
    app/super-admin/models/page.tsx
  </files>
  <action>
Refactor `model-registry-table.tsx` to use TanStack Table while preserving the existing generation grouping feature.

**Generation grouping approach:**
The current table groups models by generation (Claude 4.6, 4.5, 4). With TanStack Table, implement this as:

Option A (recommended): Use TanStack Table's grouping feature (`getGroupedRowModel`, `getExpandedRowModel`) with a `generation` column as the group-by key. Each generation header row expands to show individual models.

Option B: Keep the current visual grouping by rendering generation section headers as non-data rows between data rows. This is simpler but doesn't use TanStack's built-in grouping.

Go with Option A if it preserves the visual appearance. If TanStack grouping produces awkward visuals, fall back to Option B.

**Column definitions:**
1. `displayName` — Model name. Sortable, filterable
2. `modelId` — Model ID string in monospaced. Sortable
3. `generation` — Generation group (used for grouping). May be hidden as column but used as group key
4. `status` — Badge: green "Active", amber "Deprecated". Sortable, filterable
5. `thinkingType` — "Adaptive" / "Extended" / "None". Badge
6. `maxOutputTokens` — Formatted number
7. `pricing` — Show input/output price formatted as $/MTok (existing formatting pattern)
8. `actions` — Three-dot menu: Edit, Deprecate, Delete. Uses existing model-registry-form.tsx modal for edit, existing ConfirmationDialog for deprecate/delete

**Preserve existing functionality:**
- Add Model button opens ModelRegistryForm modal (existing component)
- Edit row action opens ModelRegistryForm in edit mode
- Deprecate uses ConfirmationDialog (existing from Phase 4 04-13 fix)
- Delete uses ConfirmationDialog
- All API calls go to /api/super-admin/models/*
- Toast notifications on success/error

**Styling:** Match other admin tables (same border, hover, header styles from DataTable component). Generation group headers should have subtle background differentiation (e.g., slightly darker bg-muted).

Update the models page to use the new table component. Ensure it passes the correct data and handles loading/error states.

Also run `npm run build` to verify the entire project compiles successfully.
  </action>
  <verify>
    <automated>cd /c/Python_project/Antigravity_folder/chatbot_ui && grep -c "@tanstack/react-table" components/admin/model-registry-table.tsx && grep -c "generation\|group" components/admin/model-registry-table.tsx && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Model Registry table uses TanStack Table. Generation grouping preserved. Build compiles without errors. All table styling consistent with other admin tables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Super Admin Dashboard (Phase 5) with:
1. Route restructure: All pages at /super-admin/* (was /admin/*)
2. Grouped sidebar navigation (Management, Monitoring, Configuration)
3. Organizations management page (DataTable + CRUD)
4. Super Admins management page (DataTable + CRUD)
5. API Keys management page (add, test, assign to orgs, delete)
6. Platform Settings page (general settings + feature toggles)
7. Platform System Prompt editor
8. Analytics dashboard (KPI cards + 9 chart sections with Recharts)
9. Audit Logs page (server-side pagination, filters, CSV/JSON export)
10. Model Registry upgraded to TanStack Table with generation grouping
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Navigate to /super-admin/login and log in as Super Admin
3. Verify sidebar shows 3 groups with 8 items, all enabled
4. **Models page**: Table shows models grouped by generation, sorting/filtering works, Add/Edit/Deprecate/Delete actions work
5. **Organizations page**: Table shows orgs with status badges, create/edit/suspend/activate/delete actions work via modals
6. **Super Admins page**: Table shows SAs, create/edit/delete works, cannot delete self
7. **API Keys page**: Can add key with name, paste API key, assign to orgs, test key validity, delete
8. **Settings page**: General settings + feature toggles with explicit save button
9. **System Prompt page**: Can edit and save platform prompt, reset to default works
10. **Analytics page**: KPI cards show, charts render, time range controls work, refresh button works
11. **Audit Logs page**: Table loads with server-side pagination, filters work, CSV/JSON export downloads files
12. Verify /admin/ paths return 404 (old paths removed)
13. Verify no console errors on any page
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Model Registry uses TanStack Table with generation grouping
2. `npm run build` compiles without errors
3. All 8 sidebar sections load their respective pages
4. All DataTables have sorting, filtering, pagination
5. All CRUD operations work with appropriate modals and confirmations
6. Analytics charts render correctly with real data
7. Audit log export produces valid CSV/JSON files
8. No old /admin/ paths remain functional
</verification>

<success_criteria>
- Every Phase 5 page is accessible and functional
- All TanStack Tables consistent in styling and behavior
- All Recharts render correctly
- Build compiles without errors
- Human verification passes all 13 checks
</success_criteria>

<output>
After completion, create `.planning/phases/05-super-admin-dashboard/05-08-SUMMARY.md`
</output>
