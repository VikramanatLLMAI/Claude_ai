---
phase: 13-functionality-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/settings-modal.tsx
  - app/api/user/preferences/route.ts
  - app/api/user/settings/route.ts
  - .planning/phases/13-functionality-audit/13-CONTROL-INVENTORY.md
  - .planning/milestones/v1.0-REQUIREMENTS.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-04

must_haves:
  truths:
    - "Every interactive control in Super Admin, Org Admin, and Settings Modal has been catalogued with its backend endpoint"
    - "Non-functional admin controls are removed from the UI with tracking in inventory document"
    - "User settings controls that lacked backend persistence now have working server-side storage"
    - "All 5 known v1.0 tech debt items are verified resolved or fixed"
    - "Stale REQUIREMENTS.md entries are corrected (SUI-01, OUI-01, OTHM-01-04 marked complete; OBRN-02/03/04 marked dropped)"
  artifacts:
    - path: ".planning/phases/13-functionality-audit/13-CONTROL-INVENTORY.md"
      provides: "Complete control inventory for all 3 dashboard surfaces with pass/fail/fixed/removed status"
      min_lines: 100
  key_links:
    - from: "components/settings-modal.tsx"
      to: "/api/user/preferences"
      via: "fetch calls for font size, code theme, appearance"
      pattern: "fetch.*api/user"
---

<objective>
Code-first audit of every interactive UI control across Super Admin dashboard, Org Admin dashboard, and User Settings modal. Build a complete control inventory, cross-reference each control with its backend API endpoint, fix all mismatches found, implement missing backend persistence for user settings, verify and resolve all 5 known v1.0 tech debt items, and update stale documentation entries.

Purpose: Ensure every button, toggle, form, and modal in the application actually does what it appears to do, with zero known non-functional controls remaining.
Output: Control inventory document, fixed source files, corrected REQUIREMENTS.md entries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-functionality-audit/13-CONTEXT.md
@.planning/phases/13-functionality-audit/13-RESEARCH.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code scan — build complete control inventory across all 3 dashboard surfaces</name>
  <files>
    .planning/phases/13-functionality-audit/13-CONTROL-INVENTORY.md
  </files>
  <action>
Systematically scan all interactive UI controls across the three dashboard surfaces and build a control inventory document.

**Scan methodology (use grep/ripgrep for each surface):**

1. **Super Admin pages** (`app/super-admin/` + `components/admin/` SA-specific):
   - Find all onClick handlers, form submissions, Switch toggles, DropdownMenu items, Dialog triggers
   - For each control found, identify the fetch() call or handler function
   - Cross-reference with the API route file to confirm: (a) route file exists, (b) correct HTTP method exported, (c) handler returns expected shape
   - Cover all 10 page directories: organizations, api-keys, models, users, super-admins, analytics, audit-logs, settings, system-prompt, login

2. **Org Admin pages** (`app/org/[slug]/admin/` + `components/admin/` OA-specific):
   - Same scan methodology as Super Admin
   - Cover all 13 page directories: users, roles, invitations, analytics, audit-logs, conversations, instructions, mcp, settings, security, branding, usage, overview

3. **User Settings Modal** (`components/settings-modal.tsx` — 1590 lines, 8 tabs):
   - Catalog every control in each tab: Profile, General, Appearance, API Keys, MCP, Instructions, Sessions, Advanced
   - For each control, determine persistence mechanism: localStorage-only, API-backed, or both
   - Flag any control that uses localStorage but has NO server-side persistence
   - Map each API-backed control to its specific endpoint (GET/PUT/POST/DELETE)

**Output format** — Create `13-CONTROL-INVENTORY.md` with sections:

```
## Super Admin Controls
| Page | Control | Type | API Endpoint | Method | Status |
|------|---------|------|-------------|--------|--------|

## Org Admin Controls
| Page | Control | Type | API Endpoint | Method | Status |
|------|---------|------|-------------|--------|--------|

## User Settings Controls
| Tab | Control | Persistence | API Endpoint | Method | Status |
|-----|---------|-------------|-------------|--------|--------|

## Issues Found
- [list of mismatches, missing backends, dead controls]

## Removed Controls
- [tracking removed controls per user decision]
```

Status values: pass (works), fail (broken), fix-needed (mismatch found), no-backend (UI only), removed.
  </action>
  <verify>
    <automated>test -f ".planning/phases/13-functionality-audit/13-CONTROL-INVENTORY.md" &amp;&amp; grep -c "Status" ".planning/phases/13-functionality-audit/13-CONTROL-INVENTORY.md"</automated>
  </verify>
  <done>Complete control inventory exists with every interactive control catalogued across all 3 surfaces. Each control has a documented status. All issues are listed in the Issues Found section.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all mismatches — implement missing backends, remove dead controls, resolve tech debt</name>
  <files>
    components/settings-modal.tsx,
    app/api/user/preferences/route.ts,
    app/api/user/settings/route.ts,
    .planning/milestones/v1.0-REQUIREMENTS.md,
    .planning/phases/13-functionality-audit/13-CONTROL-INVENTORY.md
  </files>
  <action>
Work through every issue identified in Task 1's control inventory. Fix each issue with an atomic commit.

**Fix categories (in priority order):**

1. **User settings missing backend persistence** (AUDIT-02):
   - For each settings control flagged as localStorage-only (likely: font size, code theme), implement server-side persistence
   - Use the existing User.preferences JSON field in Prisma schema (already exists for theme mode)
   - Add the missing preferences to the existing `/api/user/preferences` endpoint (or create if needed)
   - Update settings-modal.tsx to call the API on save (in addition to localStorage for instant UI)
   - Per user decision: "User settings controls must ALL work — implement the backend rather than removing the control"

2. **Non-functional admin controls** (AUDIT-01, AUDIT-04):
   - For each admin control with status "no-backend" or "fail": REMOVE the control from the UI
   - Per user decision: "Remove non-functional UI controls (buttons, toggles, forms that have no backend) — except user settings"
   - Track each removed control in the inventory's "Removed Controls" section

3. **Backend-frontend mismatches** (AUDIT-04):
   - For each control with status "fix-needed": fix the mismatch (wrong endpoint, wrong method, wrong payload shape, wrong response handling)
   - Atomic commit per fix

4. **Verify 5 known v1.0 tech debt items:**
   - Console.log debug statements in chat route — grep `app/api/chat/route.ts` for console.log, remove if present (Phase 11 DEBT-02 should have fixed this — verify)
   - TypeScript `as any` cast on tenantDb.usageRecord.aggregate() — check `app/api/org/[slug]/admin/usage/route.ts`, verify minimized per Phase 11 DEBT-03
   - Pre-existing TypeScript error: tenantDb.artifact type unknown — check `app/api/artifacts/[id]/route.ts`, fix if still present
   - Rate limiting TODO in find-org route — grep `app/api/auth/find-org/route.ts` for TODO, remove if present (Phase 11 DEBT-01 should have fixed this — verify)
   - Stale REQUIREMENTS.md entries — read `.planning/milestones/v1.0-REQUIREMENTS.md`, update: mark SUI-01, OUI-01, OTHM-01 through OTHM-04 as `[x] Complete`; mark OBRN-02, OBRN-03, OBRN-04 as `[x] Dropped`; fix SUI-01 description from `admin.llmatscale.ai` to `super-admin.llmatscale.ai`

5. **Update control inventory** with final statuses after all fixes applied. Change "fix-needed" and "no-backend" entries to "fixed" or "removed" as appropriate.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | head -20; echo "---"; grep -r "console\.log" app/api/chat/route.ts 2>/dev/null | head -5 || echo "No console.log in chat route"; echo "---"; grep -c "removed\|fixed" ".planning/phases/13-functionality-audit/13-CONTROL-INVENTORY.md" 2>/dev/null || echo "0"</automated>
  </verify>
  <done>All mismatches fixed with atomic commits. User settings all have backend persistence. Non-functional admin controls removed and tracked. All 5 tech debt items verified resolved. v1.0 REQUIREMENTS.md updated. Control inventory reflects final state with no "fail" or "fix-needed" entries remaining.</done>
</task>

</tasks>

<verification>
- Control inventory document exists with all 3 surfaces covered
- No controls with "fail" or "fix-needed" status remain in inventory
- TypeScript compilation passes (npx tsc --noEmit)
- No console.log in chat route
- No stale TODO comments for resolved tech debt
- v1.0 REQUIREMENTS.md stale entries corrected
</verification>

<success_criteria>
- Every interactive control across Super Admin, Org Admin, and Settings Modal is catalogued
- Zero non-functional controls remain in the UI (removed or fixed)
- All user settings persist to the server (not just localStorage)
- All 5 v1.0 tech debt items verified resolved
- Control inventory document complete with pass/fixed/removed status for every entry
</success_criteria>

<output>
After completion, create `.planning/phases/13-functionality-audit/13-01-SUMMARY.md`
</output>
