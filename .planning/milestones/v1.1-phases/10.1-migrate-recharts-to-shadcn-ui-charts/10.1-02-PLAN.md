---
phase: 10.1-migrate-recharts-to-shadcn-ui-charts
plan: 02
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - components/admin/org-analytics-charts.tsx
  - lib/sandpack-deps.ts
  - package.json
  - components/CLAUDE.md
autonomous: true
requirements: [CHART-04, CHART-05, CHART-06]

must_haves:
  truths:
    - "Org Admin analytics page renders all 9 Recharts-based charts using shadcn/ui ChartContainer, ChartTooltip, and ChartLegend wrappers"
    - "Org Admin charts use ExportButton from chart-utils.tsx (no duplicate ExportButton definition)"
    - "recharts is no longer a direct dependency in package.json (shadcn/ui chart.tsx brings its own)"
    - "Sandpack dependency list no longer includes recharts"
    - "Both chart files compile and build succeeds"
  artifacts:
    - path: "components/admin/org-analytics-charts.tsx"
      provides: "Org Admin analytics charts migrated to shadcn/ui"
      contains: "ChartContainer"
    - path: "lib/sandpack-deps.ts"
      provides: "Sandpack dependencies without recharts"
    - path: "package.json"
      provides: "Dependencies without direct recharts"
  key_links:
    - from: "components/admin/org-analytics-charts.tsx"
      to: "components/ui/chart.tsx"
      via: "import { ChartContainer, ChartTooltip, ... }"
      pattern: "from.*@/components/ui/chart"
    - from: "components/admin/org-analytics-charts.tsx"
      to: "components/admin/chart-utils.tsx"
      via: "import { EmptyState, ExportButton, formatTokens, ... }"
      pattern: "from.*chart-utils"
---

<objective>
Migrate all 9 Org Admin analytics charts to shadcn/ui Chart wrappers, then clean up the recharts direct dependency and update documentation.

Purpose: Complete the migration by converting the remaining chart file, remove the now-unnecessary direct recharts dependency, and update documentation to reflect the new chart library.
Output: Fully migrated org-analytics-charts.tsx, cleaned package.json, updated sandpack-deps and components CLAUDE.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10.1-migrate-recharts-to-shadcn-ui-charts/10.1-CONTEXT.md
@.planning/phases/10.1-migrate-recharts-to-shadcn-ui-charts/10.1-RESEARCH.md
@.planning/phases/10.1-migrate-recharts-to-shadcn-ui-charts/10.1-01-SUMMARY.md
@components/admin/org-analytics-charts.tsx

<interfaces>
<!-- From Plan 01 outputs - executor needs these contracts -->

From components/ui/chart.tsx:
```typescript
export type ChartConfig = Record<string, {
  label?: React.ReactNode
  icon?: React.ComponentType
  color?: string
  theme?: Record<"light" | "dark", string>
}>

export function ChartContainer({ config, className, children, ...props }): JSX.Element
export const ChartTooltip: typeof RechartsPrimitive.Tooltip
export function ChartTooltipContent({ indicator?, hideLabel?, hideIndicator?, nameKey?, labelKey?, labelFormatter?, formatter? }): JSX.Element
export const ChartLegend: typeof RechartsPrimitive.Legend
export function ChartLegendContent({ nameKey?, hideIcon? }): JSX.Element
export function ChartStyle({ id, config }): JSX.Element
```

From components/admin/chart-utils.tsx:
```typescript
export function EmptyState({ message, icon? }: { message: string; icon?: "chart" | "check" }): JSX.Element
export function ExportButton({ onClick }: { onClick: () => void }): JSX.Element
export function formatTokens(value: number): string
export function formatDate(date: string): string
export function formatMs(ms: number): string
export const ERROR_COLORS: Record<string, string>
export const INVITATION_COLORS: Record<string, string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all 9 Org Admin charts to shadcn/ui</name>
  <files>components/admin/org-analytics-charts.tsx</files>
  <action>
    Rewrite `components/admin/org-analytics-charts.tsx` to use shadcn/ui Chart wrappers. Keep all existing type interfaces and props unchanged. Keep OrgPeakUsageHeatmap, OrgUsersNearLimitsTable, and OrgInactiveUsersTable as-is (CSS grid heatmap and HTML tables -- no Recharts).

    **Import changes:**
    - Remove: `ResponsiveContainer, Tooltip, Legend, Cell` from recharts imports
    - Remove: `Button` from ui/button and `Download` from lucide-react (ExportButton now in chart-utils)
    - Add: `ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent, type ChartConfig` from `@/components/ui/chart`
    - Add: `EmptyState, ExportButton, formatTokens, formatDate, formatMs, ERROR_COLORS, INVITATION_COLORS` from `@/components/admin/chart-utils`
    - Remove: Local `CHART_COLORS`, `ERROR_TYPE_COLORS`, `INVITATION_STATUS_COLORS` arrays, local `EmptyState`, local `ExportButton`, local formatting functions

    **Per-chart migration (apply to each of the 9 Recharts charts):**

    1. **OrgUsageTrendChart** (stacked area, 3 series):
       - Same pattern as SA UsageTrendChart from Plan 01
       - `chartConfig` with inputTokens/outputTokens/thinkingTokens using `var(--chart-1/2/3)`
       - ChartContainer, solid fills with `fillOpacity={0.2}`, no gradients
       - ExportButton in CardHeader (import from chart-utils)

    2. **OrgTokensByUserChart** (stacked horizontal bar, input/output):
       - `chartConfig` with `inputTokens: { label: "Input", color: "var(--chart-1)" }`, `outputTokens: { label: "Output", color: "var(--chart-2)" }`
       - BarChart with `layout="vertical"`, `accessibilityLayer`
       - Two `<Bar>` components with `stackId="1"`, `fill="var(--color-inputTokens)"` / `fill="var(--color-outputTokens)"`
       - Dynamic height based on data length

    3. **OrgModelDistributionChart** (horizontal bar):
       - `chartConfig` with `requests: { label: "Requests", color: "var(--chart-1)" }`
       - Horizontal bar pattern, dynamic height

    4. **OrgTopUsersChart** (horizontal bar + HTML table below):
       - `chartConfig` with `totalTokens: { label: "Tokens", color: "var(--chart-2)" }`
       - Migrate only the BarChart portion; keep the HTML table below unchanged

    5. **OrgPerRoleUsageChart** (vertical grouped bar, tokens + requests):
       - `chartConfig` with `totalTokens: { label: "Tokens", color: "var(--chart-1)" }`, `totalRequests: { label: "Requests", color: "var(--chart-2)" }`
       - Vertical BarChart (not horizontal layout), `accessibilityLayer`
       - Two Bar components side by side (no stackId)

    6. **OrgMcpUsageChart** (single area):
       - `chartConfig` with `totalCalls: { label: "Tool Calls", color: "var(--chart-1)" }`
       - Single area, solid fill `fillOpacity={0.2}`, no gradient

    7. **OrgAvgResponseTimeChart** (horizontal bar):
       - `chartConfig` with `avgResponseMs: { label: "Avg Response", color: "var(--chart-3)" }`
       - Horizontal bar pattern with formatMs for XAxis tickFormatter

    8. **OrgErrorRateChart** (donut/pie with semantic colors):
       - `chartConfig` using `ERROR_COLORS` from chart-utils: `rate_limit: { label: "Rate Limit", color: ERROR_COLORS.rate_limit }`, etc.
       - Map data items to include `fill: \`var(--color-${item.type})\``
       - Remove `Cell` and `renderCustomLabel`
       - Pie with `innerRadius={60} outerRadius={100}`

    9. **OrgInvitationStatusChart** (donut/pie with semantic colors):
       - `chartConfig` using `INVITATION_COLORS` from chart-utils: `PENDING: { label: "Pending", color: INVITATION_COLORS.PENDING }`, etc.
       - Map data items to include `fill: \`var(--color-${item.status})\``
       - Same donut pattern as OrgErrorRateChart

    10. **OrgApiKeyUsageChart** (horizontal bar):
        - `chartConfig` with `totalRequests: { label: "Requests", color: "var(--chart-1)" }`
        - Standard horizontal bar pattern

    **Keep unchanged:** All type interfaces, all prop types, OrgPeakUsageHeatmap (CSS grid), OrgUsersNearLimitsTable (HTML table), OrgInactiveUsersTable (HTML table).

    **Remove:** `CHART_COLORS`, `ERROR_TYPE_COLORS`, `INVITATION_STATUS_COLORS` arrays, local `EmptyState`, local `ExportButton`, local formatting functions, all gradient `<defs>`, `renderCustomLabel`, `Cell` usage.

    Ensure `"use client"` and `/* eslint-disable @typescript-eslint/no-explicit-any */` remain at top.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 9 Org Admin Recharts charts use ChartContainer/ChartTooltip/ChartLegend. No direct ResponsiveContainer, Tooltip, Legend, or Cell usage. Heatmap and tables unchanged. Compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove recharts direct dependency and update documentation</name>
  <files>lib/sandpack-deps.ts, package.json, components/CLAUDE.md</files>
  <action>
    **1. Remove recharts from Sandpack dependencies:**
    In `lib/sandpack-deps.ts`, remove the `'recharts': '2.15.3'` entry from the dependency map. Charts are not a common Sandpack artifact use case (per user decision).

    **2. Remove recharts from package.json direct dependencies:**
    Run `npm uninstall recharts` to remove the direct dependency. Then verify that the build still works -- shadcn/ui's chart.tsx imports from recharts, but recharts should remain as a transitive dependency or be re-added if needed. If `npm uninstall recharts` removes it entirely and build breaks, re-add it as a dependency since chart.tsx needs it.

    NOTE: The user decision says "Remove recharts from package.json direct dependencies after migration (shadcn/ui Charts brings its own recharts dependency)." However, since chart.tsx is manually created (not an npm package), recharts IS still needed as a direct dependency. In that case, keep recharts in package.json but verify the import works. Update the approach: keep recharts as a dependency since our manually-created chart.tsx imports from it directly. The key change is that chart components no longer import from recharts directly -- they go through chart.tsx. Do NOT remove recharts from package.json if it would break the build.

    **3. Update `components/CLAUDE.md`:**
    Find the line `| **Charts** | Recharts v3.7.0 |` in the Quick Reference table and change it to `| **Charts** | shadcn/ui Charts (Recharts v3.7.0 wrapper) |`.

    Find `| \`analytics-charts.tsx\` | 28KB | Super Admin analytics:...` entry and update description to mention shadcn/ui Charts instead of Recharts.
    Find `| \`org-analytics-charts.tsx\` | 40KB | Org Admin analytics:...` entry and update similarly.

    In the External Resources section, replace the `[Recharts](https://recharts.org)` link with `[shadcn/ui Charts](https://ui.shadcn.com/docs/components/radix/chart)`.

    **4. Run full build verification:**
    Run `npm run build` to ensure the entire application builds successfully with the migrated charts.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Sandpack deps no longer reference recharts. components/CLAUDE.md references shadcn/ui Charts. Full production build succeeds. All chart imports flow through chart.tsx wrapper.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds (full production build)
- `npm run lint` passes
- Org Admin analytics page (`/org/{slug}/admin/analytics`) renders all charts
- Super Admin analytics page (`/super-admin/analytics`) still renders all charts
- recharts reference removed from sandpack-deps
- components/CLAUDE.md updated to reference shadcn/ui Charts
</verification>

<success_criteria>
- org-analytics-charts.tsx uses ChartContainer for all 9 charts
- No gradient defs remain in org-analytics-charts.tsx
- Heatmap and table components unchanged
- Sandpack deps cleaned
- Documentation updated
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-migrate-recharts-to-shadcn-ui-charts/10.1-02-SUMMARY.md`
</output>
