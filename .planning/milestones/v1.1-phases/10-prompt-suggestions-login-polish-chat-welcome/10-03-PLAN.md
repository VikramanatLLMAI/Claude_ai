---
phase: 10-prompt-suggestions-login-polish-chat-welcome
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - components/login-page.tsx
  - components/org-login-page.tsx
  - components/find-my-org.tsx
  - components/admin/branding-editor.tsx
  - app/org/[slug]/admin/branding/page.tsx
  - app/org/[slug]/login/page.tsx
  - app/page.tsx
autonomous: false
requirements:
  - LOGIN-01
  - LOGIN-02
  - LOGIN-03
  - LOGIN-04

must_haves:
  truths:
    - "Bare domain login (FindMyOrg) uses two-column layout with hardcoded platform branding on left and email-first form on right"
    - "Org login page uses the same two-column layout with customizable branding on left and email/password form on right"
    - "Both login pages share consistent visual design (same column widths, typography, card styling)"
    - "Org Admin can edit login headline, badge, description, and 4 feature cards from Branding admin page"
    - "Branding admin page has side-by-side live preview that updates as admin types"
    - "Org login branding fetched server-side and passed to client component (no FOUC)"
    - "On mobile, two-column layout stacks to single column (branding hidden or above form)"
  artifacts:
    - path: "components/admin/branding-editor.tsx"
      provides: "Branding editor with live preview"
      min_lines: 150
    - path: "app/org/[slug]/admin/branding/page.tsx"
      provides: "Branding admin page"
      min_lines: 10
    - path: "components/find-my-org.tsx"
      provides: "Redesigned bare domain login with two-column layout"
      contains: "grid"
    - path: "components/org-login-page.tsx"
      provides: "Redesigned org login with two-column layout and branding"
      contains: "loginBranding"
  key_links:
    - from: "app/org/[slug]/login/page.tsx"
      to: "lib/services/login-branding-service.ts"
      via: "server-side fetch of login branding"
      pattern: "getLoginBranding"
    - from: "components/admin/branding-editor.tsx"
      to: "app/api/org/[slug]/admin/branding/route.ts"
      via: "fetch for load and save"
      pattern: "api/org/.*/admin/branding"
    - from: "components/org-login-page.tsx"
      to: "lib/icon-map.ts"
      via: "rendering feature card icons"
      pattern: "getIcon"
---

<objective>
Redesign all login pages (bare domain + org login) with consistent two-column layout, create the Branding admin page with live preview editor, and wire server-side branding data to the org login page.

Purpose: Provide a professional, consistent login experience across all entry points with admin-customizable branding for org login pages.
Output: Redesigned login pages, branding admin page with live preview, server-side branding data flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-prompt-suggestions-login-polish-chat-welcome/10-01-SUMMARY.md
@components/find-my-org.tsx
@components/org-login-page.tsx
@components/login-page.tsx
@app/org/[slug]/login/page.tsx
@app/page.tsx
@lib/icon-map.ts

<interfaces>
<!-- From plan 01 outputs -->

From lib/icon-map.ts:
```typescript
export interface FeatureCard {
  icon: string
  title: string
  subtitle: string
}
export function getIcon(name: string): LucideIcon
export function getIconNames(): string[]
```

From lib/services/login-branding-service.ts:
```typescript
export function getLoginBranding(organizationId: string): Promise<LoginBranding | null>
export function upsertLoginBranding(organizationId: string, data: {...}): Promise<LoginBranding>
```

From app/api/org/[slug]/admin/branding/route.ts:
```typescript
// GET returns: { loginHeadline, loginBadge, loginDescription, loginFeatureCards }
// PUT accepts same fields, returns updated
```

From app/org/[slug]/login/page.tsx (existing server component pattern):
```typescript
// Currently fetches: org.settings?.loginTagline, org.settings?.loginWelcomeMessage
// Pass to OrgLoginPage as props
// Will also fetch LoginBranding and pass to component
```

From components/org-login-page.tsx (existing props):
```typescript
// Currently receives: orgName, orgSlug, logoBase64, logoDisplayMode, tagline, welcomeMessage, activeTheme
// Will add: loginBranding prop
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Login Pages with Two-Column Layout</name>
  <files>
    components/find-my-org.tsx,
    components/org-login-page.tsx,
    components/login-page.tsx,
    app/org/[slug]/login/page.tsx,
    app/page.tsx
  </files>
  <action>
1. **Create a shared login layout pattern** used by both login pages. Both pages follow the same two-column structure:
   - Left column (hidden on mobile, shown on lg+): branding panel with gradient/colored background
     - Logo(s) at the top
     - Headline text (large, bold)
     - Badge/tagline (small pill/badge element)
     - Description paragraph
     - 4 feature cards in a 2x2 grid (each card: icon + title + subtitle)
   - Right column: white/dark background with centered login form
   - Use `grid grid-cols-1 lg:grid-cols-2 min-h-screen` for responsive layout
   - On mobile (below lg): only the right column (form) is shown

2. **Redesign components/find-my-org.tsx** (bare domain at `/`):
   - Left column: Hardcoded platform branding:
     - Logo: "LLMatscale.ai" text logo or SVG
     - Headline: "Enterprise AI Chat Platform"
     - Badge: "Powered by Claude"
     - Description: "Deploy intelligent AI assistants to your organization with enterprise-grade security, role-based access control, and complete data isolation."
     - 4 Feature cards (hardcoded):
       1. { icon: "Shield", title: "Enterprise Security", subtitle: "End-to-end encryption and data isolation" }
       2. { icon: "Users", title: "Team Management", subtitle: "Role-based access with granular permissions" }
       3. { icon: "Zap", title: "Powered by Claude", subtitle: "Latest Anthropic models with streaming" }
       4. { icon: "Globe", title: "Multi-Tenant", subtitle: "Isolated workspaces for every organization" }
   - Right column: Keep existing email-first flow (find org by email):
     - Title: "Sign in to LLMatscale.ai"
     - Email input + "Continue" button
     - "Find My Organization" link
     - Existing logic for auto-redirect if session exists
   - Left panel uses a subtle gradient background (e.g., `bg-gradient-to-br from-slate-900 to-slate-800 text-white` in light mode, slightly different in dark mode) or a solid muted background that works with theme
   - Import getIcon from lib/icon-map.ts for rendering feature card icons

3. **Redesign components/org-login-page.tsx**:
   - Accept new prop: `loginBranding: { loginHeadline?: string, loginBadge?: string, loginDescription?: string, loginFeatureCards?: FeatureCard[] } | null`
   - Left column: Uses LoginBranding data with fallbacks:
     - Logo: Based on logoDisplayMode -- PLATFORM_AND_ORG shows both, ORG_ONLY shows just org logo
     - Headline: `loginBranding?.loginHeadline || orgName` (fall back to org name)
     - Badge: `loginBranding?.loginBadge || null` (only show if set)
     - Description: `loginBranding?.loginDescription || "Welcome to your AI-powered workspace"` (generic fallback)
     - Feature cards: `loginBranding?.loginFeatureCards` if array has 4 items, otherwise show generic defaults similar to bare domain but org-focused
   - Right column: Keep existing email/password form:
     - Title: "Sign in to {orgName}"
     - Email + password inputs
     - "Sign in" button
     - "Forgot password?" link
     - Existing auth logic
   - Same two-column layout structure as find-my-org.tsx
   - Import getIcon from lib/icon-map.ts for feature card icons
   - Remove dependency on old OrgSettings.loginTagline and loginWelcomeMessage props (they are now replaced by loginBranding)

4. **Update app/org/[slug]/login/page.tsx** (server component):
   - Import getLoginBranding from "@/lib/services/login-branding-service"
   - After fetching the org, also fetch: `const loginBranding = await getLoginBranding(org.id)`
   - Pass `loginBranding` prop to OrgLoginPage component (serialized as plain object)
   - Keep existing org settings fetch for activeTheme (still needed)
   - Remove passing loginTagline and loginWelcomeMessage from OrgSettings (replaced by loginBranding)

5. **Update components/login-page.tsx**:
   - This component is used for the legacy `/chat` page login. It may not need the full two-column treatment. If it's still actively used, apply the same two-column layout with platform branding. If it's only used as a fallback redirect, leave it as-is. Check `app/chat/page.tsx` to determine usage. If it renders LoginPage, apply the redesign. If not, skip.

6. **Styling details for both login pages**:
   - Left panel background: Use a dark gradient (`bg-gradient-to-br from-slate-900 to-slate-800`) with white text. In dark mode, use a slightly muted variant.
   - Feature cards: `bg-white/10 backdrop-blur-sm rounded-xl p-4` for glassmorphism effect on dark bg
   - Form side: Clean white bg (`bg-background`) with card container for the form
   - Right panel center the form vertically: `flex items-center justify-center`
   - Form width constrained: `max-w-sm w-full mx-auto`
   - Subtle "Powered by LLMatscale.ai" footer on right panel bottom
  </action>
  <verify>
    <automated>cd C:/Python_project/Antigravity_folder/chatbot_ui && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Both login pages (bare domain and org) use consistent two-column layout. Bare domain has hardcoded platform branding. Org login uses LoginBranding data from server-side fetch with appropriate fallbacks. Mobile responsive (stacks to single column).</done>
</task>

<task type="auto">
  <name>Task 2: Branding Admin Page with Live Preview</name>
  <files>
    components/admin/branding-editor.tsx,
    app/org/[slug]/admin/branding/page.tsx
  </files>
  <action>
1. Create app/org/[slug]/admin/branding/page.tsx:
   - Client component ("use client")
   - Standard admin page pattern: check auth, redirect if not admin
   - Render AdminPageHeader with title "Branding" and description "Customize your organization's login page appearance"
   - Render BrandingEditor component
   - Pass orgSlug from URL params

2. Create components/admin/branding-editor.tsx:
   - "use client" component
   - Accept props: `{ orgSlug: string }`
   - On mount, fetch GET /api/org/{orgSlug}/admin/branding to load existing branding
   - State management for all branding fields:
     ```typescript
     const [headline, setHeadline] = useState("")
     const [badge, setBadge] = useState("")
     const [description, setDescription] = useState("")
     const [featureCards, setFeatureCards] = useState<FeatureCard[]>([
       { icon: "Shield", title: "", subtitle: "" },
       { icon: "Zap", title: "", subtitle: "" },
       { icon: "Globe", title: "", subtitle: "" },
       { icon: "Users", title: "", subtitle: "" },
     ])
     ```

   - **Layout**: Two-column grid (`grid grid-cols-1 xl:grid-cols-2 gap-8`)
     - Left column: Edit form
     - Right column: Live preview (sticky, `sticky top-6`)

   - **Edit form** (left column):
     - Card with sections:
       - "Headlines" section:
         - Headline input (max 200 chars, placeholder "e.g., Your AI Workspace")
         - Badge/tagline input (max 100 chars, placeholder "e.g., Powered by AI")
       - "Description" section:
         - Textarea (max 500 chars, placeholder "Describe your workspace...")
         - Character count
       - "Feature Cards" section:
         - 4 card editors, each with:
           - Icon selector dropdown (using getIconNames(), render each with icon preview)
           - Title input (max 50 chars)
           - Subtitle input (max 100 chars)
     - Save button at bottom: PUT /api/org/{orgSlug}/admin/branding
     - Show toast on success/error

   - **Live preview** (right column):
     - Render a scaled-down version of the org login page left panel
     - Use the same branding panel styling as org-login-page.tsx
     - Update instantly as admin types (React controlled state -- no debounce needed)
     - Show inside a bordered container with rounded corners and shadow to look like a device frame
     - Scale down with `transform: scale(0.75)` or similar to fit the preview area
     - Show headline, badge, description, and 4 feature cards with icons
     - If fields are empty, show placeholder text in a lighter color to hint what will appear

   - **Icon selector**: Use a dropdown/select component:
     - Each option shows: `<IconComponent className="size-4" /> {iconName}`
     - Use the curated list from getIconNames()
     - If @/components/ui/select exists, use it. Otherwise use a native select styled with Tailwind.

3. Import getIcon, getIconNames, FeatureCard from "@/lib/icon-map"
4. Import AdminPageHeader from the admin components (follow pattern from other admin pages like instructions/page.tsx)
5. Use toast from sonner for save feedback
6. Handle loading state while fetching branding data (show skeleton)
  </action>
  <verify>
    <automated>cd C:/Python_project/Antigravity_folder/chatbot_ui && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Branding admin page accessible at /org/{slug}/admin/branding. Editor shows form on left with all branding fields (headline, badge, description, 4 feature cards with icon selectors). Live preview on right updates instantly as admin types. Save button persists to database via API.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual Verification of Login Pages and Branding Editor</name>
  <what-built>
    Complete login page redesign with two-column layout (bare domain + org login), branding admin page with live preview editor, and admin-customizable login page content.
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000 -- should see two-column layout with platform branding (left) and email-first form (right)
    2. Visit http://localhost:3000/org/{your-org-slug}/login -- should see two-column layout with org branding (left, may show defaults) and email/password form (right)
    3. Login as Org Admin and navigate to Admin Console > Branding
    4. Edit headline, badge, description, and feature cards -- verify live preview updates in real-time
    5. Save and revisit the org login page -- verify your customizations appear
    6. Resize browser to mobile width -- verify layout stacks to single column (form visible, branding hidden or above)
    7. Test with both logoDisplayMode settings if possible (PLATFORM_AND_ORG shows both logos, ORG_ONLY shows just org logo)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>Human verification checkpoint -- no automated action needed.</action>
  <verify>Visual inspection by user per steps above</verify>
  <done>User approves login page design and branding editor functionality</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Bare domain login shows two-column layout with hardcoded platform branding
3. Org login shows two-column layout with customizable branding
4. Branding admin page loads, edits, saves, and shows live preview
5. Mobile responsive -- stacks to single column
6. Server-side branding fetch (no FOUC on org login page)
</verification>

<success_criteria>
- All login pages (bare domain and org login) use consistent two-column visual design (LOGIN-01)
- Org Admin can customize login page tagline/badge via Branding admin page (LOGIN-02)
- Org Admin can customize login page welcome/description text via Branding admin page (LOGIN-03)
- Branding admin page has side-by-side live preview (LOGIN-04)
- Visual verification by user confirms professional appearance
</success_criteria>

<output>
After completion, create `.planning/phases/10-prompt-suggestions-login-polish-chat-welcome/10-03-SUMMARY.md`
</output>
